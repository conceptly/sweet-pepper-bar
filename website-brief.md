# Sweet Pepper — Website brief

*Working decisions for the website. Changes often.*
*System source of truth: `design.md` — brand essence, full colour ramps, logo, motifs, photography live there, not here.*

Gastrobar in Yaroslavl (Kirova 10). "Shake & cook": a serious all-day kitchen fused with a craft cocktail bar — open 8:30 till late.

## Copy and contact interactions — author decisions, 15 September 2026

Recorded after the first Home copy review (`home-copy-review-en.md`; RU review `home-copy-ru-review.md`; clean working copies `home-copy-en.md` and `home-copy-ru-draft.md`). These are editorial decisions and deferred work, not a claim that the build has been updated.

- **Keep current Home hero headlines for now.** Other copy suggestions are candidates for the final copy pass. Horseradish direction: invite the brave to meet Yaroslavl's hot side; candidate “For the brave — a taste of Yaroslavl's hot side.”
- **Form and booking copy stay untouched until final review.** Their wording reflects earlier interaction choices; do not simplify it automatically from generic UX-copy advice.
- **Calling versus copying is contextual.** The author reports that the mobile call button calls; desktop copying may make more sense. Preserve the intended distinction and verify each phone surface separately when revisiting implementation, rather than assuming every instance uses the same behaviour.
- **The copyable request is intentionally lightweight.** There is no online booking system now. A composer with day/time choices was tried and felt like too many actions for a copyable line. Keep the current example and interaction; a composer is a future possibility, not part of this copy pass.
- **Canonical website address: Kirova 10/25, Yaroslavl / Ярославль, ул. Кирова, 10/25.** Kirova, 10 is familiar shorthand used elsewhere; use 10/25 for website contact copy going forward.
- **Email domain — decided (author, 17 September 2026): `hello@sweetpepper.bar`.** The candidates were `sweetpepper.bar` and `sweetpepperbar.ru`; the site had drifted to two spellings of the second (`hello@sweetpepper.ru` in the footer, `hello@sweetpepperbar.ru` in eight other places). Every template and the report now carry the `.bar` address; the mailbox name `hello` stands.
- **Social posts are placeholders by design.** Instagram/VK routing may depend on the guest's location; that is a future decision. Account usernames and destination URLs will be checked with final copy, not resolved by guessing now.
- **Connector revisions remain SVG work.** The author will replace the repeated “THAT'S ICONIC”; do not automatically adopt the entire proposed connector sequence or replace SVGs with text. **Sequence adopted (author, Sep 2026):** AT SWEET PEPPER → **FOR A WELL-EARNED POUR** (was MORE FROM MENU) → **FOR A PROPER APPETITE** (was the first THAT'S ICONIC) → **MORE THAN A MENU** (was the second) → SEE WHAT'S NEW → JOIN THE PARTY, as drawn in both home frames (293:7465 night, 175:2214 day). The author exported PROPER APPETITE and MORE THAN A MENU from the frames (day and night, word and reflection); installed under the home `-bottom` / `-top` names. WELL-EARNED POUR was exported the same day through the Figma MCP from two new utility frames on the Desktop page, *Home connectors (desktop exports)* and *(flattened for export)*, the About pair's twins. Exported as SVG through the Figma MCP from two new utility frames on the Desktop page, *Home connectors (desktop exports)* and *(flattened for export)*, the About pair's twins: day word Olive 2px, day reflection Lime 1px, night word Cream 1px + fade, night reflection Paper 1px + fade, all at Molot 81.9 fitted to the 1092 content width. The `moreFromMenu` and `thatsiconic` files stay on disk, unused. The repeated-connector note above is closed by this.
- **Home bar/kitchen connectors — subsequent selection:** **FOR A WELL-EARNED POUR / FOR A PROPER APPETITE**. The longer kitchen phrase replaces COME HUNGRY to avoid excessive connector height when scaled to the container width. RU working pair: **ДЛЯ НАСТРОЕНИЯ И АППЕТИТА / ПЕРЕКУСИТЬ И ЗАКУСИТЬ**, explicitly provisional until final copy review. Recorded in `connector-copy.md`; author handles SVG changes.
- Remaining open questions are deferred to the final copy review. No immediate form, routing or website changes requested.

## About copy — author selections, 16 September 2026

- **Step-free**: short label inside the circular access stamp. Expanded title/detail are separate and remain for final copy review; do not put “Step-free entrance” inside the circle.
- **IN THE VERY HEART OF THE BEST CITY**: keep “very” and “best city,” change only the opening preposition from “At” to “In.” Affectionate local pride, already tested successfully in social copy. RU direction: **«В самом сердце любимого города»**.
- **WANT TO JOIN THE FAMILY?**: keep. “Family” is a local joke and an atmosphere description with real roots in relationships formed at the venue. The author reports couples meeting here and a recent wedding after more than five years together. This context explains the line; individual stories still require consent before publication. Do not replace the headline with generic recruitment wording in a later pass.
- Recorded in `about-copy-review-en.md` and `about-page-copy.md`. Selection/documentation only; no build or Figma changes in this pass. Form and booking copy remain on hold.

## Platform


**WordPress (self-hosted) + a fully custom theme + Advanced Custom Fields (ACF).** Not Webflow: Webflow is a US company bound by OFAC sanctions, cannot serve Russian accounts or payments, and Webflow-hosted sites don't reliably surface to visitors physically in Russia. Self-hosted WordPress has no foreign vendor in the loop and can be hosted anywhere, including Russia — the resilient choice given the client's market.

The theme is hand-coded to this spec in full — day/night mechanic, spice slider, image ratio system, section striping. WordPress is the admin layer underneath; it doesn't constrain the design.

**ACF scopes day-to-day editing to the team**, not the designer, without exposing layout to accidental breakage:
- Hours — an ACF options page (day-by-day fields, plus a repeater for holiday exceptions)
- Menu items — a custom post type (name, price, photo at 3:2, category, dietary tags)
- News — a custom post type (photo, caption, category, date — see below)

Note: ACF has an active free fork, Secure Custom Fields, following a dispute between WP Engine and Automattic. Both work; SCF avoids the WP Engine dependency if that becomes a concern later.

### Theme type — classic PHP, not a block theme (decided Aug 2026)

**A classic PHP theme** — `functions.php`, `header.php` / `footer.php`, page templates, `template-parts/` — not a block theme. Block themes (FSE, `theme.json`, Gutenberg templates) hand layout control to the editor, which is the opposite of what a spec this tight is for. Here the team edits *content* (hours, dishes, news) and never layout; a `theme.json` is carried only to colour the editor, not to build pages.

**Build tooling: Vite for the theme's CSS and JS.** Modern JS, hot reload, tokens compiled from `design.md` — the pleasant part of the framework world, taken without the architecture. Templates stay PHP.

### PHP stays thin (decided Aug 2026)

**PHP is a data layer, not an expressive one.** Everything that carries the design — layout, state, motion, theming — lives in CSS and JS, where the author works fastest. PHP's whole job is to fetch content and hand it to markup. This is an architectural constraint, not a preference: it's what keeps the build inside the author's competence and keeps the theme readable by whoever inherits it.

- **Templates contain a loop, an `if`, and holes.** No business logic in a template file. A template pulls its data at the top and the markup below is HTML with values printed into it.
- **Logic lives in small named functions in `inc/`,** called from templates — never inlined into markup.
- **`get_template_part()` with args is the component call.** Template parts take their data as arguments, like props; they never reach for globals or query the database themselves.
- **The daypart engine is JS, not PHP.** It has to be client-side anyway (the page cache would otherwise serve the wrong hour), so it never becomes PHP to maintain. Same for the day/night peek, the drawer, the jump-nav panel and all motion.
- **Escape on output** — `esc_html()` / `esc_url()` / `esc_attr()` at every hole, no exceptions. It's the one PHP habit that isn't optional.
- **Where PHP does more than fetch** (the hours model, the status-line lookup, schema output), it's a named function in `inc/` with one job, not logic scattered across templates.

### Plugin cap — four, each justified in writing (decided Aug 2026)

The 2016-era failure mode this project is designed against is not one bad plugin; it's the absence of a rule about adding them. **Four plugins, named here. Anything proposed later must displace one, in writing, in this section.**

1. **ACF (or SCF)** — hours options page, `dish` CPT fields, News fields. The content model depends on it.
2. **Polylang** — RU/EN structure for posts, taxonomies and ACF fields (see Top nav → EN/RU switch). *Under review (Sep 2026): the "one document, two languages" plan below may replace it with a rewrite rule — see Content editing & languages.*
3. **A page cache** — chosen at build; must be compatible with the client-side daypart rule above.
4. **Backups** — non-negotiable on a self-hosted install the author maintains.

- **No page builder, no theme framework, no plugin that emits front-end markup or CSS.** Nothing installed may fight the theme's styles, because nothing installed should produce any.
- **Auto-updates off for plugins; update on staging first.** Automatic core/plugin updates are the current default and are the one genuinely *new* risk since the author's last WordPress project.
- SEO and analytics are deliberately *not* on the list — meta tags and Restaurant schema come from the theme, off the same ACF hours data as the footer. Revisit only if the client needs an editing UI for meta.

### Repository — the `wip` branch is the backup (decided 17 Sep 2026)

Until that day only the style-guide docs were in git; the theme, the build report and every copy file existed on one disk. They are now tracked — on branch **`wip`** of `github.com/conceptly/sweet-pepper-bar`, not on `main`. The author's rule stands: a commit to `main` is a release, and nothing unfinished goes there (tablet refinement, ACF extraction and the RU pass are still ahead). A commit to `wip` is a save point, and the routine is `git add -A && git commit` on `wip`, then `git push`, at the end of a working session. `main` takes a merge only when the author calls a phase finished.

What is in: the theme complete — source, `dist/` (Local serves from it and a fresh clone runs without Node), `assets/images` (the site's selects, not the shoot — `photos/` stays ignored), `acf-json/`; `report.md`; all copy drafts and reviews; `docs/`; `case-study/`; the new `design/` icons, favicon scale and section links. What is ignored, and why: `figmaScreenshotws/` (regenerable from the file, and it grows), `Claude outputs/` (scratch), `Molot-fontfacekit/` and its zip (the licensed kit — the served `.woff` is tracked, see `design.md` §3.1 → Licences), `design/old-chili-peppers-pattern/` (licensed stock, not used by the site), `case-study/.~lock.*` (LibreOffice lock files). Seven older screenshots under `figmaScreenshotws/ticketExamples/` predate the rule and stay tracked, harmlessly.

Before any `git add -A`: scan the staged list for lock files, licensed material and anything over a few MB — the first backup caught a lock file and the stock pattern that way. Re-exporting a photo or the 8 MB case-study deck adds its full size to the history every time, so batch those.

### Content editing & languages — plan (Sep 2026)

**State at the time of writing:** every string on the site is typed into the PHP templates — no ACF field groups (`acf-json/` is empty), no queries, the `dish` CPT registered but unused. Nothing is editable yet and nothing is translatable yet. The phase after the mobile breakpoint is therefore *content extraction*, and the two questions "how does the team edit" and "how does RU work" are one question with one answer.

**Not everything gets converted.** Content is sorted into three tiers, and each tier gets the editing surface that fits it — not one editor pretending to do all three:

1. **Rows → WordPress admin.** Menu items, hours, team members, "How it feels" quotes, News. Anything that is *a list of things* with fields. A manager changing a price or pulling a dish out of season wants a table, not a canvas. The menu is the biggest item and the least scary: the 239 dish rows already exist as `get_template_part('dish-row', [...])` calls with named args (name, price, quantity, description, icons, seasonal label, options, highlight) — that is already a data model. The component stays as it is and gets fed from a loop; the rows move from PHP arrays into `dish` posts (or one ACF repeater per section on a Menu admin page — friendlier for a team new to WordPress; decide at build). **Migration is a script that parses the arrays, not hand entry.** Bilingual is cheap here: name and description get RU/EN twins, price, photo and tags are shared — one record per dish, never two.
2. **Prose → ACF fields, edited in admin first and in place later.** About story, hero leads, section intros, section photos. **ACF is the store** (decided 2026-09-10 — the earlier "keyed store" idea is dropped: it would have meant owning storage, revisions and an admin form that ACF already provides). One field group per page template, located on *Page Template = X*, one Tab per section, labels and instructions in Russian; the block editor is switched off for pages (`use_block_editor_for_post_type`) so the field form *is* the edit screen. Templates print through a helper — `sp_field('about_hero_lead', $fallback)` — that returns the field value (current English as fallback until filled) and, for logged-in editors only, adds `data-sp-field` for the in-place layer. **Field groups are generated, not clicked:** per section, the agent reads the template, emits the `acf-json/` group and swaps the strings; the author reviews the diff. The manual work is deciding what is editable and reviewing, not building.

   **The in-place editor is a layer on top of the same fields**, ported from `style-guide-edit.js` and built only if the admin form proves annoying: WordPress login (editor assets enqueued for `edit_pages` users only), a theme REST route that checks the capability and calls `update_field()`, the other-language twin while typing, Enter = `<br>`, paste as plain text, dirty count, one Save, a last-modified conflict check, cache purge on save, last-ten-values history per field. Scope beyond text: **images** via the native `wp.media()` picker (upload, choose from library, alt text) saving an attachment ID to an ACF image field, ratios enforced by hard-cropped theme image sizes, not by the picker; **small controls** (true/false, select) as a three-field popover; **lists** (dishes, people, quotes) are never edited in place — a pencil deep-links to the record in admin, "+ add" opens the admin new-record screen. No moving, adding or hiding sections; desktop only. Strings that are UI (buttons, labels, aria) are neither: `__( '…', 'sweet-pepper' )` + `.po/.mo`.
3. **Structure → code, never editable.** Section order, the filmstrip, image ratios, connectors, the daypart engine, everything under *PHP stays thin*. A field is a promise the team may change something; don't make promises that shouldn't be kept.

**Language model — one document, two languages (leaning, not final).** One page per template, both languages in the keyed store and in the dish fields; PHP renders **one** language per request from the URL segment (`/` = RU, `/en/`). This is *not* how the style guide does it — the guide ships both languages in the DOM and hides one with CSS, which is fine for a document nobody indexes and wrong for a site Yandex and Google will crawl. "One document" means one *source*, not both texts on one URL. The EN/RU pill becomes a link to the twin URL (the first-visit `navigator.language` proposal and the persisted choice stay as specified in Top nav). **Consequence for the plugin cap:** Polylang's job shrinks to routing and hreflang; a rewrite rule and a `<link rel="alternate">` loop in the theme can do both, which would free the slot. Decide during the About extraction (the save route differs), and record the displacement above if it goes.

**Order:** mobile breakpoint → tablet breakpoint (done Sep 2026 — Grid → Tablet) → ACF extraction of the About page (generated field groups, `sp_field()` helper, block editor off, one-language render) → hours options page (feeds slider, footer, schema; emitted as JSON for the daypart engine) → menu migration script → team, quotes, contacts → Polylang decision → in-place editor layer, if wanted.

**Side experiment — Bricks, on a copy of the About page.** The author will rebuild About in Bricks to feel it out; that is how solutions get found here, and it is allowed. Conditions: Bricks is a theme, so it runs in a **separate local WordPress install**, never in this repo; nothing it emits is pulled back "just for one page"; time-boxed to a day or two; a licence is paid only after checking the refund window. The test is scored on questions written before it starts, or "feels nice" wins by default: can the day/night tokens and `data-daypart` drive its elements instead of fighting them; can dish-row exist as a component with properties the team instantiates; what does its markup look like next to ours, and does outline → fill survive as a rule or become per-element hand work; can a manager change a paragraph *without* being able to change the layout (Bricks' pitch is that they can — the wrong affordance for this team); can one page carry RU and EN without duplicating the page. Two failures and the answer is specific, not "it wasn't for us." Rejection already on record in *Rejected: Tilda and low-code* applies unless the test says otherwise.

**Open (content editing):**
- `dish` CPT vs. one repeater per section for the menu — pick by which admin screen the team finds easier, not by purity.
- Field labels and instructions in **Russian**; team members get the Editor role, not admin.
- Home hero headline size is tuned to English string lengths (`design.md` §3.3 → "Home hero is the exception"; RU runs 10–15% longer) — bites in this phase, not the mobile one.
- Cache invalidation on every in-place save and on every dish/hours edit; the page-cache choice must expose a purge hook.
- Forms: `contact-form.php` and `team-form.php` need a handler in `inc/` + mail route (SMTP on the host); a forms plugin would break the cap.
- Which images are team-replaceable (media library, ACF image fields) vs. theme assets (`assets/`, code only): food and people → team; shaker, pattern, icons → theme.
- After this phase content lives in the database, not git: the deploy pipeline treats the DB as production state; backups (plugin #4) become load-bearing.

### Rejected: headless (Astro / Next) — Aug 2026

Headless means WordPress stays the admin and the database but stops rendering; a JS framework builds the pages from its API. Evaluated seriously, because the site doubles as a portfolio piece for a Canadian audience.

- **Next rejected outright.** React everywhere, for a site that is text, photographs and four interactive mechanics. It wants a Node host, and the frictionless one (Vercel) is a US company — the same OFAC problem that ruled out Webflow above.
- **Astro was the credible one:** static HTML output, zero JS by default, islands for the interactive mechanics only, hosts on any plain RU server with no Node, no foreign vendor in the loop.
- **Rejected anyway, on operations rather than taste.** Two systems to maintain instead of one. Content changes wait on a rebuild — a manager fixing a price and refreshing for a minute is a bad failure mode for a team still learning the CMS. And handover narrows from "any WordPress freelancer in Yaroslavl" to the author, permanently.
- **The day/night and daypart mechanics are client-side JS in every option.** No framework makes them easier; that is not an argument on either side and shouldn't be used as one later.
- **The Astro appetite is real and goes elsewhere:** the case-study site (`case-study/`) is the place to build it — same learning, no client risk.

### Rejected: Tilda and low-code — Aug 2026

Considered as the RU-market answer to the Webflow rejection above — Russian company, no sanctions exposure, Zero Block freeform canvas, custom code blocks, source export on the Business plan. **Rejected on the design, not the politics.** No theme-token layer for the day/night mechanic; no custom content model for menu items carrying section, dietary, seasonal and special tags; no component-with-properties for the six image ratios or the Lemon caption label; and the three tappable grammars (Interaction rule) stop being structurally enforceable and become something re-applied by hand, block by block, until they drift. Every mechanic would arrive as custom JS bolted into a builder that doesn't want it — more code than the theme costs, with less control over it.

Second reason, recorded so it isn't reopened: the project exists to produce a real web design piece. A builder caps both what can be built and what can be shown.

## News/social feed — content plan

Content goes into the News post type manually, by the team, mirroring what they already post to Instagram and VK — not pulled live via API.

- Instagram's Graph API requires Business Verification + App Review — a heavier process for a business tied to a blocked jurisdiction, with no guarantee of a smooth review.
- A Russia-hosted server may sit behind the same network-level blocks affecting ordinary users reaching Meta's API — a live pull could silently fail at the infrastructure level, independent of any API permissions.
- The team already writes this content in-house for Instagram/VK; pasting it a second time into WordPress costs seconds and has zero dependency on either platform's API surviving.

Despite Instagram being blocked in Russia since 2022, VPN adoption is high (~41–46% of Russian users) and Instagram retains real reach among Sweet Pepper's audience — so it stays a genuine content source, just not a live technical integration.

If partial automation is wanted later: VK's simpler, non-sanctioned API (register an app, `wall.get` with a token) is realistic to auto-pull from a Russian server. Instagram content would stay manual.

**Launch phasing (decided Jul 2026)** — the News/Events page is deferred so the launch stays small and the team learns WordPress on low-stakes content:
1. **Launch:** no News page, no feed. The home section ships as a static *social entrance* — own-design colour-block section, 3–4 evergreen bar photos, chips out to VK (leading) and Instagram. News drops out of the nav until its page exists (no dead nav items). Zero API dependencies at launch.
2. **Phase 2:** News CPT + manual cards on home (this *is* the team's WordPress practice — pasting posts they already wrote for VK/IG). Events page follows if the content cadence proves out.
3. **Phase 3:** importers as crons feeding the News CPT **as drafts** (human approves; automation is an importer behind our components, never an embedded widget). Two sources, same pipeline:
   - **VK** — `wall.get` direct from the RU server (non-sanctioned, simple).
   - **Instagram** (added Jul 2026 — the site doubles as a portfolio project for a Canadian audience, so automated IG is worth pursuing): a small proxy *outside* Russia (edge function) holds the token and pulls posts; WP cron fetches from the proxy. Importing self-hosts the images, so IG content becomes visible to RU guests too — the one approach that serves both audiences. Route: Instagram API with Instagram Login (Basic Display is dead since Dec 2024); risk is Meta's app review for a RU-tied business + token upkeep. If access fails, VK alone feeds the pipeline.
   
   **Never ship a live Instagram embed** — blocked for RU visitors without VPN regardless of API status; the importer supersedes it for every purpose, portfolio included.

## "How it feels" — reviews pipeline (decided Sep 2026)

Spec for the section itself (word cloud + quote wheel, tiers, one clock) is in `about-page-copy.md` → How it feels. This section is **where the words and quotes come from** and how the build is split. Same principle as the News feed: an importer behind our components, never an embedded widget; a human approves what ships.

### Why not live

- **Yandex** (the real source — ~690 reviews, ~1,600 ratings) has no official API that returns review text; Geosearch returns org data and rating only. The only sanctioned display is Yandex's iframe widget, which fails the design on every axis (no Molot, no outline→fill, no clock) and is the embedded widget we don't ship.
- **Google** Places API returns max five reviews per place; the Business Profile API returns all of them but needs OAuth as the bar's Google account, quota approval, and a Google API call from a Russian server — the same infra risk flagged for Meta.
- The tally moves maybe two words a year. A nightly scrape that can silently break on a markup change is a liability for a section that only needs re-counting a few times a year.

**So: the cloud is a periodic tally, the wheel is curated. Neither is a runtime dependency.** The site has zero external calls in this section.

### Honesty contract

"Size = frequency" is only true if the count runs over the *whole* Yandex corpus, not a sample. Tiers come from real counts, thresholds recorded in the data file, re-run at least twice a year and after every season. If the corpus ever can't be pulled, the copy must change ("words guests use"), not the sizes. Nobody hand-picks a word into a tier.

### Pipeline (three parts, three owners)

| Part | Lives in | Runs | Who |
|---|---|---|---|
| **Pull + count** | `tools/reviews/` (Python) | by hand, ~2×/year | author |
| **Store** | `sweet-pepper-theme/data/how-it-feels.json` (committed) + ACF for quotes | on commit | author |
| **Render + motion** | `template-parts/about/how-it-feels.php`, `src/js/how-it-feels.js`, `about.css` | runtime | theme |

Nothing new on the plugin list: counting happens outside WordPress, quotes use ACF we already have, the JSON is a theme asset read by a named function in `inc/`.

### Part 1 — pull and count (`tools/reviews/`)

1. **Pull.** One-off export of all Yandex reviews for the organisation (text, date, rating, author initial only — no names stored). First run: an Apify Yandex-reviews actor or a small Selenium script; either way it is a *tool*, not part of the site, and may break — that's acceptable at this cadence. Output: `tools/reviews/raw/yandex-YYYY-MM.jsonl`, git-ignored. Google/2GIS optional later, same shape.
2. **Count** (`tools/reviews/count.py`): lemmatise Russian with `pymorphy3`; keep adjectives, adverbs and a short allow-list of nouns that are feelings, not objects (уют, душа → yes; суп, коктейль → no); drop a stop-list (`stoplist-ru.txt`: dish names, staff names, "вкусно/вкусный" and other food quality words that would swamp the ambience vocabulary — the section is about how it *feels*, not how it tastes). Merge spelling/lemma variants by hand in `merge.json` (уютно/уютный → уютно). Output: `tally.csv` — lemma, count, share of reviews mentioning it.
3. **Tier.** Three brackets by count, thresholds chosen per run so the shape is roughly 3–4 top / 7–9 mid / rest low, 16–25 words total, hard cap 25. Record the thresholds in the JSON (`meta.thresholds`) so the sizes can be audited.
4. **Translate.** EN display form per word, written by the author; the count is Russian-only. Words with no honest single-word English equivalent (душевно) get the nearest word, and the JSON keeps the RU lemma as the key so the tally is never re-run on translations.
5. **Map words → quotes** by hand: each word lists the IDs of curated quotes that use it (or its idea). Every word needs ≥1 quote; the #1 word needs ≥2 (repeat-click deals the next quote).

### Part 2 — data

`sweet-pepper-theme/data/how-it-feels.json`:

```json
{
  "meta": { "source": "yandex", "pulled": "2026-09", "reviews": 688,
            "thresholds": { "top": 40, "mid": 12 } },
  "words": [
    { "key": "уютно", "ru": "уютно", "en": "cozy", "count": 131, "tier": "top", "quotes": ["q03","q11"] }
  ]
}
```

Quotes live in ACF (an options page group *How it feels*, or a `review` CPT if the wheel grows past ~15): quote RU / quote EN (Polylang-aware), platform (Yandex / Google / 2GIS), review date, link to the review or the org page, house footnote RU/EN (optional — "soulful is their word, not ours" is our voice, one line, no duplicates), and a stable ID the JSON refers to. Curation rules: 8–15 cards; short excerpts, never whole reviews; platform + date always shown; no author names; nothing that names a staff member without asking them.

**Refresh rule for the wheel (decided Sep 2026).** The wheel holds a fixed cap of 12 cards. On every site update (and at least quarterly) the author reads reviews posted since the last entry's date and admits up to **three** — only ones that add a word the cloud has, or say an existing word better than a card already in. Each admission retires one card so the count holds: retire the oldest first, and undated/relative-date Google cards before dated Yandex ones. A quote whose word isn't in the cloud waits for the next tally; it doesn't get a word invented for it. Every card keeps platform + date + link; a footnote is optional and is house voice, one line. `about-reviews.md` is the ledger — each candidate carries its added/retired date so the wheel's history is auditable. The #1 tally word must always have ≥2 cards.

**Scroll affordance and mechanics (Sep 2026)** — studied in `design-examples/how-it-feels-rail-studies.html` (four rail treatments, shared mechanics; open in a browser). Decisions so far: no pagination dots. Hover on a word is *preview* (stroke thickens, its cards get a Chili rule) — fill stays reserved for the committed word, per Motion language; click commits. The rail scroll-snaps; whichever card settles at centre fills its word, so the two columns are one state. Edge cards are cropped by the viewport and pull to centre on click; only the centred card opens its review link. Keyboard: the rail is focusable, ↑/↓ step cards. **The rail loops** (decided Sep 2026): three cloned sets in the DOM, viewport starts on the middle set; on settle (`scrollend`, with a debounced-`scroll` fallback) on a clone, disable snap, jump with `behavior: 'instant'` (the viewport's `scroll-behavior: smooth` would otherwise animate it), re-enable. Clones are `aria-hidden` with `tabindex="-1"` links; word↔card maps and `is-centred` point at originals only; tilt parity by global index; the heartbeat steps to the next DOM neighbour, not the next original.

`inc/how-it-feels.php`: one function `sweet_pepper_how_it_feels_data()` that reads the JSON, pulls the ACF quotes, joins them, filters out words whose quotes are missing, and returns one array. Templates get that array; they don't read files.

### Part 3 — render and motion (the theme)

Template: `how-it-feels.php` already exists with hard-coded arrays — replace the arrays with the `inc/` call, keep the markup shape. Each word carries `data-key`, `data-tier`, `data-quotes`; each card carries `data-id`. The default state is server-rendered (#1 word filled, its first quote centred) so the section is correct with JS off and before hydration.

`src/js/how-it-feels.js` (new, registered in `main.js`) owns everything that moves — per `about-page-copy.md` and Motion language:
- one clock (~3.5 s) that fills the next word *and* rolls its quote to centre;
- click on a word: seize the clock (pause ~8 s), step the word to the cloud column's centre, fill it, dim the others to ~30 %, roll its quote in; repeat-click deals the next quote from its list;
- click on an edge card pulls it to centre and fills its word;
- gentle drift + pointer parallax with tier-weighted depth (top words lead); transforms only;
- `prefers-reduced-motion`: drift, parallax and auto-advance off; fill and quote swap remain.

CSS stays in `about.css`: tier = size (64 / 40 / 26 per the copy doc — **open**, they don't fit the column; see `about-page-copy.md` → Cloud layout candidates → Sizes); **hue = a dealt six-stop palette, not the tier (revised Sep 2026)** — Chili, Deep Chili, Paprika, Olive, Avocado, Ash, dealt round-robin by the template as `data-hue`, stable across visits, Paprika top-tier only for contrast; the section never themes, so one set. State = outline vs fill. No new colours.

#### Cloud layouts B (Field) and E (Pile) — implementation notes (Sep 2026)

Two candidate layouts from the cloud studies (`about-page-copy.md` → How it feels → Cloud layout candidates). Reference code: `design-examples/how-it-feels-cloud-studies/Field.dc.html` and `Pile.dc.html`, method `setup()` — plain JS with no dependencies; port it into `how-it-feels.js`, don't include the study files. Everything below is *in addition to* the rules above; nothing above is relaxed.

**Shared — markup, no-JS state, timing**
- PHP renders the same flex rows as today (row-grouped words, `data-key`, `data-tier`, `data-quotes`, the #1 word `is-active`). Rows are the no-JS and pre-hydration state for every layout. A modifier on the cloud (`about-cloud--field` / `about-cloud--pile`) chooses the layout; JS only ever adds to the rows. Never server-render absolute positions.
- On init JS measures every word's box *in the row layout* (`offsetWidth` / `offsetHeight`), and only then switches the container to `position: relative` with a fixed height — **the column wrapper's height (it stretches to the quote rail), never the row layout's**: rows pack words tightly and a spiral needs roughly twice that area, otherwise most words find no legal spot and stack on the centre (the Sep 2026 "pile" bug). Floor it at 480 px; shape the spiral to the container's aspect; tighten the gap, then the inset, before ever falling back to the centre, and warn in the console when that still happens. Then the words go to `position: absolute; left: 0; top: 0`, driven by `transform: translate()`. Words are `visibility: hidden` until the first placed frame so they never flash at 0,0.
- Measure only once Molot is usable: `document.fonts.load('56px Molot')` with a 1.5 s cap — not `document.fonts.ready`, which waits for every face on the page (Golos from the network included). Molot is `font-display: swap`, so measuring too early gives fallback-face widths and the layout is wrong for the whole visit. Also wait until the container has a width > 0 — a node that isn't attached yet, or sits in a `display: none` tab, measures 0.
- Re-measure and re-place on `resize` (debounced ~200 ms). ~~Below 768 px both layouts fall back to the rows~~ **Phones run the field (author, Sep 2026):** the box is 370 wide with a 380px floor (`FIELD_MIN_H_PHONE`), 16 words at 40 / 26 / 18 place with no overlaps down to 343 wide (a 375 phone, checked under reduced motion), orbit at half amplitude (3–6px) so 12px neighbours never touch, and commit is fill + dim only — the rows stay the no-JS and pre-hydration state.
- Colours are `data-hue` rules in `about.css` (six stops, see Part 3 above); no night set — About is a fixed composition and the section is light. The active word gets `z-index: 2` so it is never behind another, mid-flight included.
- One rAF loop per section, transforms only, `will-change: transform` on the words; pause the loop off-screen (`IntersectionObserver`, the same one that pauses the clock).
- Hover on a word = stroke thickens to 2.5 px, `@media (hover: hover)` only. The `scale(1.08)` hover in the current CSS is retired for these layouts — scaling fights the placement.
- Clock, seizure and rail are unchanged: the heartbeat fills the next word *and* rolls its quote, and never moves a word. A click on a word commits it (fill + centre + dim others to 30 % + quote); a click on a card commits its word the same way. The end of the 8 s pause is where the cloud returns to rest — B's words go home, E's lifted tile drops.

**B · Field**
- *Placement (once, after measuring):* sort words largest first; for each, walk a spiral from the column centre (r = s × 0.9 px, θ = θ₀ + s × 0.35, y compressed × 0.85) and take the first spot inside a 16 px inset with a 12 px gap to every placed box. Seed the start angle deterministically (a hash of the word list) so the layout is stable across visits; homes are recomputed on resize only.
- *Idle:* each word orbits its home on two slow sines (periods 9–19 s, amplitude 6–12 px); words within 170 px of the pointer lean toward it by up to 12 px. Reduced motion: no orbit, no lean.
- *Commit(i) — make room:* the cleared zone E is the word's own box centred in the column plus a 28 px margin. Run a relaxation on copies of the homes, ~200 iterations, synchronously at click time (18 words × 200 iterations is well under 2 ms): (1) pull every other word toward its home by 4 % — drop the pull for the last 80 iterations so the result is exact; (2) any word intersecting E steps 8 px radially away from the centre; (3) clamp to the inset, and if a word is still in E after clamping (a wall blocked it) step it 8 px vertically away from the centre instead; (4) pairwise AABB separation with the 12 px gap, split evenly, smaller-overlap axis wins; (5) clamp again. The order matters — the pair pass must be last, or the wall fallback leaves overlaps. Store the result as each word's *target*; the loop eases the current offset toward the target at 8 % per frame (instant under reduced motion). The active word's target is E itself.
- *Release:* targets = homes, same easing. Repeat-click on the active word deals its next quote and does not re-run the relaxation.
- *Checked (Node port of the same code, 18 words × every word centred):* no neighbour inside the zone, no pairwise overlaps, nothing outside the inset. Keep that check as a unit test whenever the word list changes — it is what "no words behind the active word" rests on.

**E · Pile**
- *Bodies:* one per word, `w = offsetWidth`, `h = offsetHeight × 0.86` (Molot's box has air above the caps), a fixed tilt of ±2.5° for the tile look — visual only, collisions stay axis-aligned. Position-based (verlet) integration: `DT = 1/120`, two sub-steps per frame, gravity 1500 px/s², velocity damping 0.98 per sub-step, x-velocity snapped to 0 below 0.02 px so the pile goes fully still. No velocity edits on contact — that is the bug the first study hit (a min-velocity chain that hung the whole pile in mid-air); positions only.
- *Spill:* bodies start above the frame, staggered 40 px + jitter per word, with a tiny random horizontal drift; the whole set lands in ~3 s. Spill on section entry, and on the "Spill again" control if it ships — a boxed button per the Interaction rule, right-aligned above the pile.
- *Constraints, 4 passes per sub-step:* pairwise AABB separation, smaller-overlap axis wins. Vertical contacts move only the upper body, damp its x-velocity (× 0.6) and, if its centre is offset more than 40 px from the body below, slide it 1.5 % toward falling off (this is what spreads the pile instead of stacking a tower). Horizontal contacts split evenly. Walls (12 px inset) and floor clamp *after* the pairs in each pass; a floor contact halves x-velocity (friction).
- *Pointer:* bodies within 90 px are nudged away (0.05 px per px of proximity horizontally, 0.02 vertically) — a hand brushing tiles, not a repulsor.
- *Commit(i):* the body is pinned: it eases to the column centre at 10 % per sub-step, is immovable in every contact (mass 0), and collides with a 20 px margin on all sides so no tile can rest against or behind it; the pinned tile renders untilted and on top. A click on another word unpins the current one first (it drops back into the pile). Never leave two pinned — that stacks invisible tiles at the centre and the pile lands on them as a shelf (seen in the first study).
- *Release:* unpin; the tile falls. Reduced motion: no physics — lay the words in rows from the floor up (same bodies, no animation) and keep fill + quote only.
- *Checked (Node port):* settles by 3 s, no overlaps, everything inside the inset. Perf: 18 bodies × 4 passes × 2 sub-steps ≈ 2,500 pair tests per frame — trivial; keep the hard cap of 25 words.

### Split of work

- **Antigravity chat "How it feels — data":** `inc/how-it-feels.php`, the ACF group (export to `acf-json/`), template hook-up, JSON shape, sample data. PHP is fetch-and-hand-over only.
- **Antigravity chat "How it feels — motion":** `how-it-feels.js` + the CSS states. Start from the server-rendered default; add the clock, then click, then drift/parallax. Test reduced-motion last.
- **Claude Code (cloud, network):** `tools/reviews/` — the pull + `count.py` + first `tally.csv` and first `how-it-feels.json`. Runs where Python and the network are; the theme never runs it.
- **Author:** stop-list, merges, translations, quote picks, footnotes. This is copy, not code.

### Open
- CTA: `about-page-copy.md` says chip; the template and Figma show a boxed button. Pick one (Interaction rule allows either; a button is louder — decide against the rest of the About page's CTA set).
- "YOUR" in the current word list is a stray from the placeholder set — confirm it disappears with the real tally.
- Whether to add Google/2GIS to the corpus, or keep Yandex as the single source and just credit the others on quotes.
- Quoting: platform + date + link is our attribution floor; check the client is comfortable quoting guests on the site before launch.

## The Pepper Story — heat-line timeline + counter ledger (decided Sep 2026)

Copy and anatomy: `about-page-copy.md` → The Story. Build notes: `report.md` → The Pepper Story. This section records the interaction decision.

**Four studies, one pick.** Desktop studies on a Claude Design canvas (*The Pepper Story · Interaction Studies*, 8 Sep 2026): **A** by the book (the copy doc's entrance, decorative hover) · **B** heat line (the timeline joins the outline → fill grammar) · **C** printed slip (the mobile ticket brought to desktop, printing from under the axis) · **D** poster number (deliberately wrong — one 184 px Molot numeral in a Lemon band, ticking live). **B was chosen and built.**

- **Why B.** The hover *means* something, in vocabulary the slider and the word cloud already taught: 2009 and 2014 are outlined (past), 2026 STILL HERE is filled (current), and a Chili heat line runs from Tabasco to the arrowhead — it started there and is still burning. Hover a year: it fills and the heat retracts to it; on 2009 the ledger dims to 40 % because there were no Sweet Pepper orders yet.
- **Why not C.** The Concept section already prints a bartender's ticket; a second slip on the same page dilutes the device (The bartender's ticket → max one per view, and one closing device per page). The mobile ledger keeps its ticket *edge* as a motif, not slip anatomy — same reasoning as the footer.
- **Why not D.** It breaks `design.md` §3.2 (Molot reads better at moderate sizes), §6 (one focal point — it fights the founder photo), Lemon's highlight-only role and the day page's single colour-block moment, and the no-clock principle (a live tick makes the guest feel time passing and passes an estimate off as live data). The poster scale half-earns it — the giant-Molot register carved out for connectors already exists — but the live tick earns nothing. Parked.

**Rules this section honours (and how):**
- *One clock per section* — the ledger's slot rotation (4 s) is the only idle motion; the timeline has no idle animation of its own.
- *Idle self-demo, human seizure* — hovering the ledger stops the clock; it resumes 8 s after the pointer leaves.
- *Spring easing, transforms only* — axis and heat scale on `transform`; labels pop on `opacity` + `translateY`; the one exception is the heat's `width` on hover, which is a state change, not layout motion.
- *Reduced motion* — no entrance, no rotation, final numbers written directly; fill on hover remains.
- *Colour* — axis is Ash at 35 % (time is neutral), heat and numerals are Chili (Olive numerals would read as prices, `design.md` §2.2), years Olive, wit lines Ash → Peppercorn on hover. Jokes stay in Golos; Molot names things.
- *No-JS state = finished state* — the server renders final numbers and the full heat; pre-entrance states only exist once the script has armed the section, so nothing depends on hydration.

**Entrance (once, on scroll into view):** axis draws left → right (700 ms) → cap, years, ticks, names and wit lines pop with a 120 ms stagger per marker → arrowhead lands (spring, 900 ms) → heat runs 2009 → arrowhead (900 ms) → counters count up (1.4 s, ease-out) → rotation begins if the pool has more entries than the three slots.

**Counters.** Integers in the template, formatted with a space as the thousands separator on both sides (PHP and JS). The pool is emitted as JSON; rotation is dormant until it grows past three entries, so no placeholder numbers ship. **Open:** the template carries 22 200 / 56 340 for soups and shots where the Figma frame carries 41 200 / 96 700 — confirm the kitchen-approved set with Iurii before launch.

**Geometry note.** The axis moved from 11 px (through the year labels) to 28 px, the tick's centre: years above the line, names below, ticks crossing it — the drawing in the Figma frame.

### Open
- Outlined Molot at 18 px is thin; review on the live page and raise the stroke to 1.25 px if it reads weak.
- Mobile: the Figma frame shows a segmented year control (2009 / **2014** / 2026) with one milestone shown at a time, and the ledger as a ticket-edged card. Not built; decide whether the heat line survives the switch to a segmented control or the control simply *is* the state (filled segment = current).
- Whether the current-year label rotates (TODAY / TONIGHT) stays parked with the copy doc.

## Typography on the web

Molot leads; hierarchy comes from size, colour, case, and position — never a second display face (`design.md` §3.2). Golos Text carries everything meant for reading. Four web-specific additions:

*Build note (Sep 2026): tracking on the display levels must be written as a percentage (`0.04em`), never the desktop pixel value — two H1s (menu Location, the picker title) carried a fixed 2.56px and wrapped to four lines at the phone 36.*

### Sanctioned small-size exception
The slider meal-period labels (BREAKFAST / LUNCH / DINNER / PARTY) are set in Molot at 13–18px — single words only — with a letter transition: inactive → outline → active fill. This is the one place Molot goes below display sizes.

### Interaction rule
Exactly three container styles signal "tappable":

1. **Boxed actions (buttons):** Golos Text SemiBold 16, always in a button container. Nothing else uses this style.
2. **Contained inline actions (chips):** Golos Text Medium 13 inside a chip container with an icon and/or trailing arrow, plus a hover state.
3. **Doors (menu-state switches):** a Molot word on a small tilted block with an always-visible arrow — spec in Menu page → Doors. Used *only* for crossing between the food and drinks menus; nothing else may be door-shaped.

**Chip success state (documented Sep 2026).** A chip that copies to the clipboard reports back in place: the label becomes "Copied!", the leading icon swaps to the checkmark, and the chip holds its hover colours for ~2 s. Both glyphs ship in the markup and CSS swaps them — never rewrite the chip's innerHTML, which drops the inline SVG and leaves a typeface character where the icon was (the bug this rule exists to prevent; found and fixed on the Visit map bar and the home Contacts chips, Sep 2026). **Every icon in a contact or booking control is an inlined SVG from `assets/icons/`, not the Phosphor webfont** — `design.md` §5.2 → Delivery; swept Sep 2026 across the contact form, the team form, the reserve drawer (ticket chip, phone CTA, social arrows, the three bar-state marks) and the home Contacts block.

- 🔶 **Open — two success glyphs for one action.** The address chips confirm with the **checkmark**; the `contact-item` copy chip confirms with the **fire** glyph. On the Visit page both appear in one view. Pick one and align the other.

Non-interactive text must never borrow these styles. Expressive or instructional text (e.g. "Pick your heat!") is set in Molot, tied visually to the element it points at — not in button typography.

### Line-wrapping
`text-wrap: pretty;` on all body copy (Golos Text reading content) — avoids orphans and ugly one-word last lines, keeping paragraph shape even now that the Grid section constrains body copy to a fixed measure. Browsers cap the algorithm's cost to a limited number of lines per block, which is a non-issue here since body copy is already scoped to short teasers or 2-column runs, never a single unbroken long block.

### Form text styles
A dedicated UI/Forms style group, not numbered Caption variants — keeps `Caption` scoped to its original role (captions, fine print, addresses) instead of diluting it into "whatever's small."

- **Form label** — Golos Medium 500, 13px/140%/4% tracking. Same values as Caption, distinct semantic name (e.g. field labels).
- **Form hint** — Golos Regular 400, 13px/140%/4% tracking. New weight variant; used for supporting/helper text and de-emphasized tags like "(optional)" — paired with the secondary/muted text colour (Ash on light, Mushroom on dark), never the primary text colour.

Reserve future small-UI needs (tags, table cells, tooltips) for new entries in this same group — not more Caption numbers.

Note for AI tools: Molot is a local font and will not render — substitute any heavy all-caps display sans and judge structure, not letterforms.

## Day / night pairings (web)

Full ramps, roles, and the generative colour rule: `design.md` §2. Website-state mapping:

| Role | Day | Night |
|---|---|---|
| Hero surface | Paper — photos at full saturation (wash retired, see note below) | Peppercorn (photo, dark) |
| Primary text | Peppercorn | Paper |
| Secondary text | Ash | Mushroom |
| Accent | Chili (unchanged) | Chili; Paprika/Lemon for small text |
| Eyebrow | Olive | Lime Light |

Contrast note (**re-measured Aug 2026 — the old figure was wrong**): Chili on Peppercorn is **4.47:1**, not 3.4:1. The conclusion holds but the margin is far tighter than it looked — it clears 3:1 for large Molot comfortably and misses the 4.5:1 body-text threshold by three hundredths, so it is not a safe small-text colour and shouldn't be rounded up to one. Small accent text on dark still uses Paprika or Lemon. *(The 3.4 figure appears to be Chili on **Lemon**, 3.42:1, transcribed onto the wrong pair — worth checking anywhere else that number was reused.)*

**Revision (menu-page work, Jul 2026):** the original day rule — "photo washed with Paper ~90%" — flattened the photography and made day heroes read foggy and lifeless. Retired. Day-mode photos run at full saturation (the day register is already bright per `design.md` §7); day energy comes from tilt, hard Peppercorn keylines, and the Lemon ticker band instead of a wash.

## The day/night concept

**Settled Aug 2026. The daypart grid is the only theme control; there is no day/night toggle anywhere on the site.**

The site reflects the actual time of day. Day state: cream palette, kitchen/breakfast content, menu-first CTA. Night state: dark palette, cocktails/events, reserve-first CTA. Greeting, copy, CTA pair, photography and palette all swap per daypart (breakfast / lunch / dinner / party).

**Time proposes, the guest disposes.** On load, the theme and the selected card follow the current daypart. The four photo cards in the home hero are the control: tapping one selects that daypart and the theme follows it — breakfast and lunch are day, dinner and party are night. Same mechanic at both breakpoints (Desktop — home hero, Mobile — home hero).

**Why no toggle.** Night is not a dark mode, it is the bar. A toggle would frame the theme as a *preference*, which contradicts the thing the theme is for — the room is dark after dark, and that's a fact about the venue, not a setting a guest chooses. Dropping it also removes a control that changed no information off the home page.

**The now-marker (decided Aug 2026).** The marker always sits on the card matching the real daypart, independent of what's selected (Mobile — home hero → NOW marker). It has two renderings:

- **Now card is the active one** — the default every guest lands on — the marker is a **labelled** state: the 8px Lemon dot plus `now` / «сейчас».
- **Now card is inactive** — the guest has moved — the marker is the **dot alone**, 8px Lemon, bottom-left, on the 4px grid.

**Why that way round and not the reverse.** The label sits in the state everyone sees first, so the dot is *taught* before it has to work alone; by the time a guest is displaced they have already met it. It also matches the drawing: the active card is at full strength and full size and can carry type, while inactive cards are dimmed (Mobile — home hero → Active tile marking) and would make a label muddy.

Tapping the marked card is the way home, and there is **no separate "Back to now" control** — being displaced costs the guest nothing, so a persistent control would imply a problem that doesn't exist.

### What themes and what doesn't (decided Aug 2026)

| Surface | Behaviour |
|---|---|
| **Home** | Themes. Driven by the daypart grid |
| **Menu — food state** | Themes day/night. The only inner page with two theme states |
| **Menu — drinks state** | **Always dark.** The bar's own register; it does not take a day treatment |
| **About**, **Visit** | **Fixed compositions.** Each page runs its own light and dark sections in a set order and does not theme by time |
| **Footer** | Always Lime, both modes, every page (Section striping) |

**This supersedes the Jul 2026 Visit decision** that the standalone Visit page *"themes by time like every other page — its day hero is light"*. Visit is a fixed **dark · light · dark** composition closing on the Lime footer. Section striping and `visit-page-copy.md` → Theming have both been corrected.

### The override travels on the link, not as sticky state

The hero CTA already renames itself with the selection ("Breakfast menu" / "Dinner menu"), so **that click carries the daypart** — `/menu?daypart=dinner`. Consequences:

- Arriving at the menu from the hero keeps the continuity the grid promises: pick dinner, land in the dinner menu in night clothes
- Arriving at the menu from the nav themes by time, like everything else
- **No persistent global theme state.** Nothing sticky to get lost in, and no orphaned dark page at 11am with no marker on it and no way back
- Deep links stay honest, which matters since every menu state and section anchor is specified as shareable

### Downstream

- **Edge positions.** The right edge now holds Reserve alone. The rule "edge positions keep stable meanings site-wide — left = section nav, right = act on the venue" (Top nav) should read as **right = Reserve**, and the desktop-scoping caveat it carried for the peek is moot.
- **The tiles are a state switch.** That collides with Don't (web-specific), which reserves state switches for doors. The Interaction rule needs the daypart grid added as a named sanctioned entry — the count stops being three and the *list* becomes the rule.

**Why the peek went, kept as a record.** Off the home page it toggled a palette, not content — the menu page's real axis is food ↔ drinks, which has its own control, and nothing a guest needs was reachable only in the other theme. The honest case against dropping it: day/night is a signature idea, and a guest who only visits at lunch never learns it exists — but that is a brand/portfolio argument, not a user-need one, and it lost to the toggle-implies-preference problem above. The drawer-only middle option went with it.

## Top nav (decided Jul 2026)

**Items: Home · Menu · About · Visit.** News/Events stays out until its page exists (no dead nav items — see Launch phasing).

- **Home label kept — default decision.** Logo links home too (implicit + explicit, per NN/g; «Главная» is also standing RU web convention, and Segments B/C are the logo-only-fails slice). First position. *(Open test — see `testing.md`.)*
- **Logo idle-shake** (candidate, motion language): the logo joins the idle self-demo vocabulary — an occasional shaker wiggle on the section's single idle clock — advertising its interactivity without withholding the Home affordance. Works on touch, where hover-discovery doesn't exist.
- **Social chip:** one chip, "See what's new" — VK glyph leading, IG second, links to VK (IG one step inside). Official full-colour brand marks (`design.md` §5.2) — the colour is what marks them as contacts, distinct from the single-colour supporting icons. This is also News' interim home while the page is deferred.
- **EN/RU switch:** text pill, both codes always visible, active filled, Caption size. System language proposes on first entry only (`navigator.language`); an explicit tap persists (localStorage) and is never re-overridden — the "time proposes, memory disposes" principle applied to language. RU content leads; EN/RU voice lines are register twins, never machine translations.
- **Day/night peek is not in the nav** — right-edge tab under Reserve (see The day/night concept). Edge positions keep stable meanings site-wide: left = section nav (menu page), right = act on the venue (Reserve + peek).

**Mobile header — built (Sep 2026).** Below 992px the bar is a 44px row inside 16px vertical padding (76 total), 16px gutters, 8px bottom radii, **fixed** to the viewport (scroll-away on scroll-down / return on scroll-up is a later task). Left: the Symbol at 32px plus a flat Chili "SWEET PEPPER" wordmark at 16px (Chili in both modes) — not the stamp-bearing horizontal lockup, which `design.md` §4 keeps out of small sizes. Right: a 44px hamburger target with a 24px icon, Olive by day, Lemon by night and on About/Visit (dark hero). Figma `topNavContainer` 1164:48786, "mobile, full" states only.

**About and Visit on phones — one Peppercorn bar, decided (author, Sep 2026).** These two are fixed compositions with dark and light sections under one fixed bar. Three options were weighed: a ground-aware bar that flips Paper/Peppercorn with the section beneath it (an IntersectionObserver against a header-height sentinel — modest to build, but it would flip mid-word on the nine half-cut connectors at About's seams and needs a rule for which section owns the seam); a static dark bar that scrolls away (rejected: it breaks the fixed-header rule above and puts the hamburger out of reach on an 11,000px page); and **a fixed Peppercorn bar at every section, which is what ships** — with its 8px bottom radii it reads as a bar over Parchment, not as a stray dark section. The page ground under it (`body` on those two templates, ≤ 991px) is Peppercorn too, or the corners cut a Paper notch into the hero. *Build note:* this state had been recorded as built since the mobile header landed, but the desktop "transparent, absolute" override for the same two pages sat later in `header.css` at equal specificity and won on phones too, leaving the bar static over the body's Paper offset; the desktop rule is now scoped ≥ 992px.

### Mobile — the drawer (decided Aug 2026)

Below the nav's desktop breakpoint the four items collapse into a **full-screen drawer**, opened from a hamburger at the right of the header (logo holds the left). The drawer themes by time like every other surface — Paper by day, Peppercorn by night — and is the mobile home of everything the desktop header carries, so nothing needs a second tap to reach.

**Anatomy, top to bottom:**

- **Header** — horizontal logo lockup left, close × right. The drawer is the only place a × appears in the top nav.
- **Nav list** — the four items in Molot, each with a one-line Golos description and a trailing arrow. Descriptions are guest-voice (`design.md` §1.1), not feature lists: they say what the guest gets, in the guest's "your."
- **Venue card** — a raised surface (Parchment by day, Soft Peppercorn by night) holding the route out to the map and the social row. This is where the desktop "See what's new" chip lives on mobile; the same launch-phasing rule applies — the label must not name a News/Events page while that page is deferred.
- **EN/RU pill** — unchanged from the desktop rule (both codes always visible, active filled). It lives in the drawer on mobile rather than in the collapsed header; that is a deliberate accepted cost, since the drawer is one tap and the alternative is crowding a 390px header.
- **Booking block** — a Molot instructional line ("Book your table") over one filled primary action and two outlined secondary actions. Molot here is sanctioned instructional text tied to the element it points at, not button typography (Interaction rule). **Phone leads** — RU guests book by call; messaging apps are the fallback, not the default.

**Current-item state — outline → fill, same as everywhere.** Inactive items are outlined Molot; the current page fills (Chili by day, Lime by night) and swaps its trailing arrow for a "you are here" marker. Colour alone never carries the state — the drawer must read correctly in greyscale, and outline→fill is the site's one state-change vocabulary (Motion language).

**Marker constraint** *(built with the bare chili as drawn, Sep 2026 — still open)***:** the "you are here" mark cannot be a bare chili. The logo Symbol is shaker + chili together with a 24px floor (`design.md` §4), so a lone recoloured pepper is a new mark, not a house one. Either place the Symbol at ≥ 24px or use a non-logo device (filled notch, rule, block).

**Icons:** VK and Instagram appear up to three times in this drawer. All instances are the **official full-colour brand marks** (`design.md` §5.2) — never recoloured to the surface's text colour, so they read as contacts rather than as supporting icons. *(Corrected Sep 2026; the earlier single-colour rule here was a misreading of `design.md`.)*

**Built (Sep 2026)** from `Drawer-4-Home-Day` 1341:51991 and `Drawer-4-Home` 1227:56007, as drawn — including the four items flagged below (nav order, bare-chili marker, "News & Events" label, "10/25"), which stay open. **Theme rule:** Home and Menu follow the page's day/night; **About and Visit get the dark drawer in every theme**, matching their fixed dark hero. The drawer's header row sits exactly where the site header's does (same lockup, × on the hamburger's spot), so opening reads as the bar staying put. Tap targets are 44px throughout (close, social icon buttons, all buttons).

**Open (mobile nav):**

- **Reserve on mobile — under consideration (Aug 2026), three placements tried and rejected.** The site-wide rule puts Reserve on the right viewport edge in all states; that rule does not survive a 390–402px screen. Findings on record so the ground isn't re-covered:
  - *Right-edge tab* — takes too much width and overlaps content. The edge affordance depends on a margin mobile doesn't have.
  - *Floating bottom button* — collides with the CTAs that already live at the bottom of sections (Submit, See the menu, the booking block).
  - *Persistent top-nav button* — the best of the three if Reserve must stay visible, but it crowds the header, and it goes redundant the moment an in-content Reserve is on screen (visible in the current home-hero mockup, where the nav button and the hero button sit in the same view — also two accent-filled primaries at once, against the one-primary-per-view rule).

  **Built this way (Sep 2026):** the right-edge Reserve tab is hidden below 992px; Reserve lives in the hero button, the drawer's booking block and the Contacts section. Author's leaning, now the build: no persistent Reserve on mobile. It lives in the drawer's booking block plus the sections where it's relevant, and phone leads in both. If it is later kept visible, the top nav is the placement — with the house yield rule attached: *a persistent control hides while an in-content control doing the same job is in view* (same pattern as the menu page's jump-nav, which only appears once the hero's side-nav has scrolled away). Parked until after the Visit page ships.

  **The drawer itself on phones — built (Sep 2026)** from `reserveModal-mobile` 1198:53322: a bottom sheet (≤ 767px) sliding up on the house spring, transform only, 8px top radii, 34/16 padding, 36px between blocks; the ticket moves to the end. Messenger links draw ↗ on both breakpoints (decided Sep 2026: they leave the site) — the desktop mockup's → is the one to correct. Figma draws only the night sheet — Peppercorn ground, Paprika title, Paper text, Mushroom status line. (The status line is Mushroom by night at every width now — the desktop build had it Ash on Peppercorn, 3.0:1, against the Figma `phoneState`.) The day twin is derived, not drawn: Paper ground (the mobile nav drawer's theme rule), Chili title, Olive outlines; the ticket is Parchment in both. Tablets (768–991px) keep the 420px right-edge panel. Dialog semantics added at every width: `inert` + `aria-hidden` while shut, focus to the × on open and back to the opener on close, Escape closes. Open: `Copy` sits flush with the ticket text (the Figma `cta` frame carries a second 24px inset that reads as an artefact).

- **Day/night peek on mobile** — folded into the larger question of whether the peek survives at all; see The day/night concept → Under consideration.
- **Nav order.** Decided order is Home · Menu · About · Visit; current mockups run Home · About · Menu · Visit. Menu is the highest-intent destination — restore it to second, or record the reason it moved. *Built as the mockup (Sep 2026); one array in `mobile-drawer.php` to reorder.*
- **Descriptions — voice pass.** "Hours, map, and all the contacts" is a calque and "Get Directions" is venue-voice; the house line already exists ("Your route to Pepper"). Write the four lines as register twins in RU and EN, not translations.
- **Address:** mockups say "Kirova 10/25", the docs say Kirova 10 — confirm with Iurii and fix one of them.
- **Close × colour:** Lemon makes dismissal the brightest object in the header, and Lemon is highlight-only (`design.md` §2.2). Candidate: Parchment by night, Peppercorn by day. *Built as drawn: Ash by day, Lemon by night (Sep 2026).*

## The spice slider

> **Status: superseded in the home hero, both breakpoints (Aug 2026).** The daypart tile grid replaced it on mobile first, then on desktop (see Desktop — home hero and Mobile — home hero). The slider is not in any current home-hero mockup.
>
> **This section stays, and is still load-bearing.** The state model, the closed-window parking rules, the day-type logic and the full status-line matrix below were never slider-specific — they describe *what the site knows about the hour*, and the tile grid consumes all of it unchanged. Read everything after "State model" as the daypart engine; read the paragraph immediately below as the retired instrument.
>
> Not deleted, because the decision is one round of testing old and the instrument may return on a surface with room for it (the slider reads well at desktop width; it lost on comprehension, not on fit). If it's still unused after the Visit page ships, retire the instrument paragraph and rename this section "The daypart engine."

The hero's central interactive element **(retired instrument — see status above)**: a heat slider with four stops — breakfast (mild), lunch (warm), dinner (spicy), party (on fire). Heat = time of day. It defaults to the current daypart and doubles as the day/night control. Dragging changes greeting, CTA pair, photography mood, and palette. Stop labels follow the Molot letter transition above. Planned: the knob is a pepper (or a circle containing one) that fills with red as heat rises; the giant SWEET PEPPER wordmark can fill in sync.

### Component variants (Figma)

Visual variants only — everything else is text properties, or the set explodes:

- **Active stop:** breakfast / lunch·brunch / dinner / party (4)
- **Mode:** day / night (2)
- **Marker:** at-now / displaced — now-marker visible (2)

Max 16 visual variants. The second stop's label (LUNCH ↔ BRUNCH) and every status line are **text props**, not variants.

**Gap to close:** the five states in the model below collapse to **two** visual variants plus copy — beginning / full swing / last call are the same frame with a different status line (which is why status lines are text props), leaving *displaced* and *closed* as the only ones that change the drawing. But **closed sits on no axis** in the list above. Either make Marker three-valued (at-now / displaced / closed → 24 combinations, 20 legal, since closed only exists on breakfast and party) or add it as a boolean and accept invalid combinations. Decide before building the set — retrofitting an axis means rewiring every instance.

### State model (logic layer)

- Per stop, five possible states: **beginning / full swing / last call / off-hours** (dragged to outside its window), plus **closed** on the two edge stops only (breakfast pre-open, party post-close). "Closed" is never a fifth stop — the scale enumerates offers, and closed isn't one.
- **Day-type** (weekday / weekend·holiday) comes from the ACF hours options page — same source as the footer hours; holidays follow the weekend rule via the exceptions repeater; Sunday's 10:00 opening is an hours exception, not component logic.
- **Now-marker:** Lemon track notch, visible only while the knob is displaced; hidden during closed hours (there's no "now" on the track when the room is dark).
- **Closed-window parking:** close → 05:00 parks on party (closed line); 05:00 → opening parks on breakfast (closed line; Sundays until 10:00). Overnight, the site points at the next open door.
- Status line format: [fact] — [reassurance/wink]. Golos caption. Numerals clockless style: till 12 · till 4 · 1:30 — never AM/PM.

### Status lines — weekday

| Stop | Beginning | Full swing | Last call | Off-hours | Closed |
|---|---|---|---|---|---|
| Breakfast | doors just opened — coffee's already on | the morning round — Bio Bio glass till 12 | breakfast stays all day — Bio Bio glass clocks out at 12 | eggs any hour — the morning glass returns at 8:30 | still closed — coffee and eggs are back at 8:30 |
| Lunch | lunch is on — the soup's already out | lunch till 4 — no rush | lunch till 4 — last call! | back tomorrow at noon *(Friday evening: back Monday at noon)* | — |
| Dinner | dinner's on — book for eight | the kitchen's on — no rush | still time for dinner — kitchen's good till 1:30 | the evening kitchen starts at 4 | — |
| Party | first cocktails poured — the night's warming up | the room's buzzing — kitchen till 1:30 | last order 1:30 — make it count | starts after six — you're early | closed for today — back at 8:30 |

### Status lines — weekend / holiday overrides

Second stop label → **BRUNCH** (no special menu exists — the copy frames that as generosity, pre-empting the "brunch specials?" expectation):

| State | Line |
|---|---|
| Beginning | brunch mode — the full kitchen's awake |
| Full swing | the brunch menu is the whole menu — that's the deal |
| Last call | brunch runs till four — Saturdays fill up, book or come early |
| Off-hours | brunch is a weekend thing — weekdays it's business lunch |

Sunday (10:00 opening, general cleaning):

- Closed / pre-open: "Sunday scrub — back at 10, spotless" *(rhymes with SPOTLESS in the About word cloud)*
- Breakfast beginning: "doors at 10 today — the room's just been cleaned within an inch of its life"

### To confirm / translate

- All lines need RU versions — voice copy, no machine translation.
- "Bio Bio glass" — confirm the offer's exact name and terms.
- Confirm all-day breakfast reality and weekend opening hours with Iurii, then fix `sweet_pepper_bar_brief.md` (hours section still says "Breakfast 8:30–12:00 weekdays").
- The busy-Saturday booking nudge lives only in brunch last-call — one nudge in the whole matrix, where waiting is a real risk.
- **Kitchen close time corrected (Jul 2026):** Dinner's last-call line said "kitchen till 11" — stale; kitchen actually runs till 1:30, matching Party's full-swing line. Both stops now state 1:30. Confirm this single close time covers every night, or if any night differs.
- **DJ line softened (Jul 2026):** Party's beginning/full-swing copy assumed a DJ every night ("DJ's setting up" / "DJ's on"). Revised to atmosphere-first ("the night's warming up" / "the room's buzzing") since a DJ isn't nightly and Party is really about room energy — music's on every evening regardless. If a DJ night should be called out specifically, that's a separate data-driven variant, not default copy (see note below).

## Desktop — home hero (current state, Aug 2026)

The desktop hero runs the **same daypart tile grid as mobile** — four tiles, one per daypart, tap to select, theme follows the selection. The mechanic, the state model and the status copy are shared; only the composition differs. Recorded here as *current state*, not as a settled layout: the composition has a live alternative under consideration (below).

### Composition

Single column, left-aligned, stacked top to bottom: eyebrow → headline → body copy → **a single horizontal row of four tiles** → CTA pair → language nudge → the giant Molot wordmark bleeding off the bottom of the section.

- **Eyebrow is present on desktop** — "SHAKE & COOK · YAROSLAVL", `--accent-2`. (Mobile drops it for space; this is a deliberate divergence, not drift.)
- **Headline is Chili in both modes** on desktop — **and now at every width (author, 17 September 2026: the desktop is the correct one).** The phone and tablet bento had run `--accent-2` (Olive / Lemon) from the mobile frames; the frames are the ones to re-sync. The eyebrow stays `--accent-2` where it shows (desktop and tablet).
- **Active tile** — larger, with a keyline and the offset Lemon/Chili sheets. Desktop is where the layering language belongs: it has the width to give the sheets a real offset, which is exactly what mobile can't do (Mobile — home hero → Layering stays a desktop device).
- **NOW marker** — a small pip on the tile that is now when it isn't selected; a "Now" label on it when it is. Independent of selection, same two-axis rule as mobile.
- **CTA pair** — Reserve (Chili, primary) plus a **daypart-named menu button**, each with its own glyph. The button renames itself with the selection, which is a good detail — it promises the *part* of the menu the guest is already looking at rather than "the menu."
  - **It now goes where it says (Sep 2026).** The href was a static `#menu` that exists nowhere on the page; `daypart-engine.js` carries a `btnHref` per daypart alongside `btnText`, and swaps both. Breakfast → `/menu/#breakfast`, Lunch → `/menu/#lunch`, Dinner → `/menu/#hot-dishes`, Party → `/menu/?menu=drinks#cocktails`. The PHP default matches the server-rendered "Breakfast menu" label so it is never dead before JS runs. Every anchor checked against the rendered menu page.
  - 🔶 **Label divergence — "Drinks menu" or "Bar menu"? (found Sep 2026).** This entry listed the fourth daypart label as **"Bar menu"**; the build says **"Drinks menu"** — in `daypart-engine.js` and in the Highlights header CTA, the only two places either phrase is used as a label. But **"Bar menu" is live as prose** on the menu page (`bar-snacks.php`: "See it in the Bar menu!"), so the site names the same thing two ways. This is the same class of problem as the Reserve / "Get your table" split recorded on the Visit page — decide one at the final review and align the other, rather than correcting the doc and calling it settled.
- **Mobile diverges here deliberately:** mobile carries Reserve only, with a named scroll cue doing the menu's job, because two buttons don't survive the viewport. Desktop has the room, and the menu is the highest-intent action on a restaurant site, so it keeps the button.
- **Language nudge** — first-entry only, `navigator.language` proposes and an explicit choice persists (Top nav → EN/RU switch). Currently sits bottom-right of the hero.

### Open (desktop hero)

- **Two-sided layout — the leading alternative.** Grid occupying one full side (left or right) as a column or block, copy on the other, instead of the current horizontal row under the text. Author's first instinct for the next pass, and it addresses the dead zone below directly.
- **The right third is empty.** Headline ends around 830px and the grid around 940px of a 1280px frame, so the current composition reads as the mobile stack widened rather than a desktop layout. This is the single biggest thing to fix, and the two-sided idea exists because of it.
- **The active tile's size advantage is much weaker than on mobile** — roughly 15–25% larger versus about double. Size was the primary state signal on mobile; on desktop the keyline and sheets are carrying nearly all of it. Either restore the size ratio or accept that desktop marks state differently and say so.
- **No "Back to now" anywhere.** The tiles don't auto-revert, and a displaced state (e.g. cocktails selected while the pip sits on lunch) currently has no way home. Same gap as mobile; see The day/night concept for the proposal to give the job to the edge tab.
- ~~**The language nudge is unattached**~~ — **in flow now (author, 17 September 2026).** It was absolute, hung above the hero footer, and whenever the hero's content was taller than the section's minimum it landed on the CTA buttons: measured overlapping at 991 × 744, 1280 × 700 and **1440 × 800, a 13" laptop with browser chrome**. In flow, right-aligned above the foot word, it costs ~53px of hero height and pushes the word down instead; at 1280 × 900 and 1440 × 800 the hero still ends on the fold because the centred composition absorbs it. Dismissal collapses the wrapper (already in the script), so the word rides up 53px when the nudge goes. Whether it belongs in the hero at all is still the layout question above.
- **Copy pass pending**, deliberately deferred until the layout settles. Known issues on record so they aren't rediscovered: "Lunch's till 4" isn't idiomatic (→ "Lunch till 4"); "bar offers on the go" means portable, not running; "PUMPKIN SOUP TIME!" ties a headline to one dish and breaks the day it's off the menu; and three of four headlines are exclamations with two sharing an "X TIME!" construction — the set needs variety in shape, not only in words.

## Mobile — home hero (decided Aug 2026)

Below the desktop breakpoint the hero drops the slider and runs a **four-tile photo grid** instead — one tile per daypart. Same mechanic, different instrument: heat is still time of day, the guest can still move along it, and the state model and status copy are shared with the slider.

**Why it changed.** Usability testing: a participant called the slider "too complicated," and fairly — the rail carried four labels, a knob, a now-marker, a status line and an instruction, six pieces of chrome to communicate one fact. A/B against a vertical rail: participants preferred the grid, and so does the author. A photograph of eggs also says *morning here* faster than the word BREAKFAST, and says it in both languages at once.

**What it costs, recorded honestly.** The grid shows one daypart at a time, so the all-day story — the thing that separates Pepper from a bar or a café — is no longer visible at a glance the way four labelled stops made it. Mitigations available if testing shows it matters: one tile borrowed from another daypart with a caption ("also mornings →"), or leaning on the Menu highlights section below. Not currently built.

### Anatomy

- **Four tiles, one per daypart**, in an irregular grid. The **selected tile is the large one** — size carries state, so the grid *is* the scale and no separate track is drawn.
- **Tapping a tile selects that daypart:** hero copy, CTA and photography change, and the **theme flips with it** — breakfast/lunch → day, dinner/party → night.
- **No meal labels on the tiles.** Tried and rejected: too wordy at 402px, and they fight the NOW marker, which is the more useful of the two. The hero text names the daypart instead.
- **No eyebrow on mobile.** Also tried and rejected — with 16px body copy, real image sizes and a normal gap there isn't room, and it reads wordy. The logo carries the venue name; the headline carries the daypart. (Desktop keeps the eyebrow.)
- **NOW marker** — a pip on whichever tile is *now*, **independent of what's selected**. It sits on an inactive tile whenever the guest has moved. Selection and now are two axes, not one; the slider has the same split (knob vs now-marker).
- **Active tile marking** — a 2px **Peppercorn** keyline (decided 17 September 2026 — as built and as the component draws it; this bullet had said `--accent-2`), plus the size difference. **Inactive tiles are dimmed** rather than the active one being brightened: same unlit → lit logic as outline → fill, no new device, and only one photo is ever at full strength, so an uneven photo set doesn't show four at once. *Current build uses an outline plus a glow; the glow is a selection halo that appears nowhere else in the system — restyle pending, not blocking.*
- **Layering stays a desktop device.** The offset Lemon/Chili sheets from the menu-page hero need roughly 14px of offset to read as stacked paper; at mobile tile sizes they shrink to ~4px and read as colour fringing, closer to a print misregistration than to depth. Mobile uses keyline + size only.

### Tile states

| | is now | not now |
|---|---|---|
| **selected** | large · keyline · NOW pip | large · keyline · "Back to now" appears |
| **not selected** | small · dimmed · NOW pip | small · dimmed |

### Behaviour

- The tapped tile **moves** into the large slot — never a crossfade. If the photo the guest touched dissolves, they lose track of what they just did.
- **No auto-revert.** An explicit choice is not overruled by the clock — "time proposes, memory disposes," same principle as the language switch. "Back to now" is the only way back, which argues for giving it a fixed home rather than a chip that appears and disappears in the hero.
- `prefers-reduced-motion`: instant swap, no theme cross-dissolve.
- **This is now the mobile day/night control** — tapping a dinner or party tile darkens the site. It therefore replaces the peek on mobile entirely, and feeds the open question in The day/night concept → Under consideration: the mobile hero now does what the peek tab was for.

### Colour roles in the hero

- ~~**Headline and the active tile's keyline** take `--accent-2` — Olive by day, Lemon or Lime by night.~~ **Superseded (author, 17 September 2026): the headline is Chili in both modes at every width, as on desktop.** The "red flooding the frame" concern below was weighed and the desktop composition kept; if red does flood on a phone with red-heavy photography, the answer is the photo set, not the headline. The keyline is Peppercorn at every width — decided the same day (author), see Anatomy.
- **Reserve is Chili in both modes.** One CTA colour across modes beats one optimised per mode: Lemon and Lime both fail as buttons on Parchment (too little separation from the ground), and a Peppercorn primary in day mode was rejected. Red therefore leaves the headline, which is also the fix for red flooding the frame — logo, button and headline were all Chili at once on top of red-heavy food photography.
- **The NOW pip takes the remaining accent, never the headline's.** A Lemon headline with a Lemon pip puts the largest type and the smallest state marker in the same colour, in the same viewport.

### CTAs

One button (Reserve) plus a **named scroll cue** — "What's cooking tonight ↓" — instead of a second menu button. The hero already shows four photographs of food, so appetite is served above the fold; the cue carries the menu path, which is why it must name its destination. "Scroll down for more" promises nothing and doesn't do that job.

**Condition attached:** the section below (Menu highlights) has to *open* with food. If it opens with a Molot headline and the photos sit another 200px down, a guest who scrolls once sees no menu content and the hero's missing menu CTA costs a visit.

### Cost to plan for

Four dayparts × four tiles is **16 art-directed photos** rather than the slider's four, maintained by the team in WordPress. Decide which tiles can be shared across dayparts before shooting. A bento is also only as good as its worst photo and shows several at once — the dimming rule above is partly insurance against that.

### Not carried over to mobile

- **The eyebrow** and **the second (menu) CTA** — both present on desktop, both cut here for space. See Desktop — home hero.
- **The giant Molot wordmark in the hero.** Its job is a section seam, not an introduction (Layout notes); asking it to name the venue above the fold is what made it look clipped by the browser chrome. It stays lower on the page, where its size is an asset rather than a fold problem.

### Build status (Sep 2026) — where the build and the rules above differ

Built from `home-hero-bento-stack` 2048:126962 (night) / 2048:127658 (day) and the `meal-home-container-opt-2` component 1659:35997. Decisions taken in the build session, and the rules they touch:

- **Grid-first, decided.** Tiles → headline → copy → Reserve, then the connector. Nav (76, fixed) + hero = one viewport; the content block is **centred** between nav and connector (author's call after seeing it in the browser; bottom-anchoring was tried first), never less than 24px under the nav, 36px above the connector.
- **The colour stack is on mobile — trial, against "Layering stays a desktop device".** The author chose to try the stacked cards first for consistency with the desktop hero. At the mobile size (234×156 photo, 242 container, 8px right/bottom padding) the offsets are the desktop ones, not the ~4px the rule above feared. If it stays, rewrite that bullet; if it goes, the keyline-only variant is one CSS block. The stack follows the **desktop per-daypart recipes and alternating tilt** (Desktop — home hero), not the component's single Lime/Lemon/Chili order and −1° for every daypart; the Now pill rides the tilted photo as on desktop, where the component draws it level.
- ~~**Keyline is Peppercorn, not `--accent-2`.**~~ **Decided (author, 17 September 2026): Peppercorn, as built.** The component's active photo carries a 2px Peppercorn border, the same as desktop; the Anatomy rule that said `--accent-2` is corrected above. Nothing to change in the build.
- ~~**Headline colour is Olive by day, Lemon by night — and the NOW pip is Lemon too.**~~ **Resolved (author, 17 September 2026): Chili at every width, the desktop rule.** The night clash of a Lemon headline beside the Lemon pip goes with it. The frames draw Olive / Lemon — re-sync them.
- **Headline size is a raw 32px** (Molot, 104%, 4%) in the final frame, not `Size/HomeHero` → `HeadlineFontSize` (56, Party 52). Author is keeping 32 pending notes; `design.md` §3.3 carries the same note.
- **No scroll cue in the final frame.** The frame runs Reserve only, with the "AT SWEET PEPPER" connector below it; the `opt3-link` variant (a tappable connector) was tried and dropped, `opt3-word` won. The CTA rule below (named cue + Highlights must open with food) is therefore not met yet — the scroll-cue hero variant is parked, and the Highlights condition is still owed when that section is built.
- **Tile move is a real move, on the house spring.** A tap FLIPs every tile from its old box to its new one over 800ms Gentle (position by transform; width and height animated — see the Motion language exception); dim overlay, sheets, keyline and the day/night ground cross-fade on the flat curve. Reduced motion: instant, no cross-fade. The glow is gone — dimming + keyline + size only, as the rule asks.
- **Not built:** "Back to now" (no way home after a tap, same gap as desktop), the language nudge (hidden on mobile — it has no mobile design), the eyebrow and the second CTA (cut, as specified).
- **Fits:** 402×874, 390×844, 430×932. A 375×667 phone overruns the viewport by 4px (content height).
- **The bento reaches portrait tablets (Sep 2026).** The phone block's query is now phones *or* 768–991 at `min-height: 1000px`, with a tablet block for what differs — see Grid → Tablet → *The home hero in the band*. The FLIP in `daypart-engine.js` gates on the same query.

### Open (mobile hero)

- ~~**Text-first or grid-first**~~ — resolved grid-first (Sep 2026, see Build status).
- ~~Active-tile treatment — see the glow note above.~~ — the glow is gone, the keyline is Peppercorn (decided 17 Sep 2026) and the night Lemon headline / Lemon pip clash went with the Chili headline the same day. Nothing open here.
- Whether the colour stack stays on mobile (trial, see Build status). If it does, rewrite "Layering stays a desktop device".

## Mobile — Highlights (built Sep 2026)

From `HighlightsSection` 2048:126987, as drawn. 48px rhythm throughout (desktop 80): connector → title block → card rail → the two menu CTAs, full width and stacked → connector. Cards keep their 320 width and become a horizontal snap rail that bleeds across the 16px gutters; the cropped third card is the scroll affordance — no dots, no scrollbar, same call as the About quote rail. Connectors stay the desktop SVGs scaled to 370 (Section connectors → Width matching); the Figma `SectionLinkMobile` treatment is still not adopted.

**Section title on phones — component-wide.** The rules from the Figma mobile section header (`sectionTitle-Mobile` 1164:51509, used on every mobile screen) went into the shared section-header component, so every section that uses it inherits them: title block full width, CTAs under it at full width, eyebrow colours unchanged from desktop (Olive by day, Cream by night — the frame's Avocado was a stale instance, decided Sep 2026). **Eyebrow size — decided Sep 2026:** the Figma component sets it in `Heading/Captions` (Molot 16); the build keeps **H3 18 at every width**. Tested in the browser, 18 works on a phone, it stays consistent with desktop, and it avoids adding the second small-Molot exception `design.md` §3.3 leaves open. Revisit only if 18 starts crowding a real title; the Figma component is the one to re-sync then.

**Home → menu links — the whole set (Sep 2026).** Recorded here because the convention was settled doing the highlight cards, and it governs every link from the home page into the menu.

**Only one menu state renders per request**, so the state travels with the anchor: `#infusions` does not exist in the food state — verified against the rendered page, not assumed. Food is the default, so food anchors need no query. Every destination below was checked against the live markup.

| from | destination |
|---|---|
| Card — House infusions | `/menu/?menu=drinks#infusions` |
| Card — Grilled wings | `/menu/#bar-snacks` |
| Card — Pot roast | `/menu/#hot-dishes` |
| Highlights header — Drinks menu | `/menu/?menu=drinks` |
| Highlights header — Food menu | `/menu/` |
| Bar preview — Explore the drinks menu | `/menu/?menu=drinks` |
| Kitchen preview — Explore the food menu | `/menu/` |
| Hero daypart button | per daypart — Desktop — home hero → Composition |

**All eight were dead before this pass.** The four section/preview CTAs pointed at `#bar-menu` and `#kitchen-menu`, the hero at `#menu` — none of those ids exists anywhere on the home page; the cards had no link at all. After the pass the only same-page anchors left on the home page are `#page` and `#primary`, and both resolve.

**The card is a door.** A highlight card renders as an `<a>` when it has a `url`, and as a `<div>` when it doesn't, so the component stays usable display-only. It was a `<div>` carrying `cursor: pointer` until now — it looked clickable, was not, and no keyboard could reach it. Focus gets a Chili outline by day, Lime by night.

**Card rows align by structure, not by centring (fixed Sep 2026).** `.highlight-card` was `justify-content: center`, which hung the whole stack in the middle of the equal-height box, so any difference in content height carried the photo with it — measured at 900: photos starting at dy **26 / 15 / 0**, tags at **361 / 373 / 388**. Now `flex-start` + `align-items: stretch`, the tag pinned with `margin-top: auto`, and the description clamped to three lines. Photo, title and tag share a baseline on all three cards at every width. Worth recording because it was misread as a photo-size problem: the photos were never different heights — `aspect-ratio: 3/2` on equal-width cards cannot differ — only differently placed.

**Open:**

- 🔶 **Card row alignment — the copy fix is a stopgap, settle it at the final review.** "Pepper's pot roast" was shortened to **"Pot roast"** (author, Sep 2026) so the title holds one line. That hides the symptom rather than fixing it — and moves it. The title row is the unstable part, and the **price** wraps to its own row independently of the title, on a different number of cards at every width. Measured header heights (Infusions / Wings / Pot roast):

| viewport | 768 | 900 | 1024 | 1152 | 1280 |
|---|---|---|---|---|---|
| header height | **90 / 60 / 32** | 60 / 60 / 32 | 60 / 32 / 32 | 60 / 32 / 32 | 32 / 32 / 32 |

The three cards agree **only at the 1280 design width**. Shortening the title made "Pot roast" the one card whose price never wraps, so it became the odd one out at 768–1152 instead of the tall one — the drift changed shape rather than going away, and at 768 the spread is now 58px. Photo, title and tag stay aligned throughout; it is the description that moves. Two structural fixes were built and measured. **Subgrid** (`grid-template-rows: subgrid` on the card, `grid-row: span 4`) aligns all four rows at every width, truncates nothing, leaves no dead space on rows where every title is short, and shrinks the card ~16px; verified down to the 320px phone rail with snap intact. A fixed two-line title reserve does **not** work — it still left 29px of drift at 1152, because the *price* wraps independently of the title. Subgrid needs one decision first: `.highlight-card-header` is `flex-wrap: wrap`, so the price sometimes takes its own row and sometimes doesn't; Figma specifies title and price on **one** row, and the build has diverged at every width below ~1440 since that wrap was added. Either make the stack deliberate (`flex-direction: column`) or keep the Figma row and shorten the price copy — "From 150 ₽" is the one that forces it. **Truncating the title to one line was considered and rejected:** the dish name is the product, an ellipsis in Molot at 4% tracking reads as a rendering fault rather than a choice, and RU runs longer than EN so it would fire *more* often after Polylang, not less.
- **Section description still says "Pepper's pot roast"** while the card now says "Pot roast". Prose and label can differ, but decide it rather than leaving it as a leftover.
- **Description copy.** The mobile mockup runs only the second sentence ("One of the only places in Yaroslavl…"); the build renders the desktop two-sentence version at both widths. Decide whether mobile gets the short cut, and if so whether that is a copy rule (shorter teasers on phones) or a one-off.
- **"Highlights must open with food"** (Desktop — home hero → CTA rule) is still owed: on a phone the first card lands ~380px below the section top, under the connector, the title and six lines of description.
- **Card rail on tablets (768–991px)** — answered by the Sep 2026 tablet pass (Grid → Tablet): the desktop three-up grid holds, and on the ramped gutter the cards run **229–285px**. Still in no mockup; the widths were settled in the browser, not in Figma.

## Mobile — Bar & Kitchen previews (built Sep 2026)

From `AboutSection` 2048:127019 — the layer is misnamed; it is the bar preview (Home Made Infusions). Single column at 48px section rhythm: photo first (3:2 at the full 370, caption label inset 24 instead of the desktop 16) → 36px → H2 title and the shared dish rows unchanged → 48px → the CTA at full width. The kitchen preview has no mobile frame yet; it stacks the same way (photo first), so its desktop image-left mirroring is moot on phones. Connectors stay the desktop SVGs.

**Fixed on the way, both widths:** the preview title had no night rule and rendered the day Chili; it is Lemon by night, as both the desktop night mockup and the mobile frame draw it. And the bar CTA's pepper glyph was invisible on phones: Figma exports share `clipPath` ids across files, `Pepper.svg` (the button) and `c-Pepper.svg` (the drawer's "you are here" marker) both define `clip0_52_3199`, and on phones the drawer's copy comes first in the document. Inline SVGs now go through `sweet_pepper_inline_svg()` (`inc/inline-svg.php`), which suffixes every id per instance; the button component and the drawer marker use it, the other `file_get_contents` call sites still don't — move them over as they're touched.

**Open:**

- **Caption label — settled Sep 2026.** Copy stays "Community hit!" (the frame's "Summer 2026!" was placeholder). The badge was the one off-spec instance of the label (tilted, Golos 500, radius 2); it now matches the component (Image caption label → Geometry). The dish row's own seasonal tag (`dish-seasonal-badge`) keeps its tilt — it is a different device, inline in a list, not a photo caption.

## Mobile — About preview (built Sep 2026)

From `AboutSection` 2048:127039, as drawn. One column at 36px between blocks, 48px section rhythm: eyebrow + two headline lines → photo (3:2, full 370) → description → the two stat chips (12px icons) → CTA at full width. The photo sits outside the text column in the markup, so the column is dissolved with `display: contents` and the pieces ordered in CSS, as on Highlights. Stat labels corrected to Golos 400 at both widths (they were 500; Body/Caption is 400).

**Eyebrow by night — Cream, decided Sep 2026.** This frame's title is the older `sectionTitle` (212:2387) with a Cream eyebrow; the Highlights frame drew Avocado. Cream is what the section header component specifies, so the build uses the desktop colour at every width and the Highlights frame is the one to re-sync.

## Mobile — Events / social entrance (built Sep 2026)

From `eventsSection-mobile` 2048:127051, as drawn. 48px rhythm: connector → title block → card rail → the two Follow CTAs at full width → connector (CTAs re-ordered under the rail with the same `display: contents` device as Highlights). Cards are the fixed **240×300** `instagram-feed-cards-mobile` in a snap rail across the 16px gutters, five cards plus the "See all events" tile (blurred photo under a 75% Peppercorn scrim, Lime link text by night). The phone card is `instagram-feed-cards-mobile` 1260:40711 in all three states. Day: Parchment ground with the house card shadow, Peppercorn title, Soft Peppercorn caption date, overlay fading to Parchment by 43%. Night: Peppercorn ground, Lime date, Paper title. Pinned by night: 2px Lemon keyline, no glow (the desktop glow stays desktop). The desktop card's hover crossfade has no touch equivalent, so on phones the default layer carries the affordance: the date row trails the **source's full-colour brand mark** (VK / Instagram, 16px, decorative — the whole card is the link), the body overlay drops to 16px padding (82px tall), the category pill sits 16 from the corner (Image caption label → Geometry), and the **title runs one line with an ellipsis — intentional (decided Sep 2026):** titles may come from social posts, so their length can't be planned for; the card's height is the fixed thing. Section-header rules apply unchanged; the description stays the component's **Paper** for now — the frame's Mushroom was a deliberate try at calming it, to be judged on a real phone in a browser, one token to change.

**Open:**

- **Trailing icon in the card component.** Two of its three states (day, night-pinned) trail the Instagram mark; night-default trails a 12px arrow. Built with the brand mark in every state — an icon that changes with the theme would read as a bug — and the arrow means "this word is the action" (Interaction rule), which the mark avoids. Re-sync night-default in Figma, or say the arrow is wanted and it's one line to flip.
- **Launch phasing still applies:** the section ships as the static social entrance — chips out to VK and Instagram, no News page behind "See all events" (it links to the VK community). The rail is ready for Phase 2 cards without changes.

## Mobile — Contacts (built Sep 2026)

From `Contacts` 1198:53132, as drawn. **The form is gone on phones** (author's call: it made the section too long); the desktop keeps it until said otherwise. In its place, top to bottom at 24px: the title block with a **phone-only short description** ("Walk-ins always welcome…", Body/Secondary 14 — the section-header component now takes a `description_mobile` that replaces the desktop paragraph ≤ 767px) → the reserve drawer's **booking block** in the flow (phone CTA on the bar-state engine, then VK / Instagram half-width, then "Usually answer in 20 minutes") → a **"Get directions" band** bleeding across the gutters (map **360–400px by viewport** — `clamp(360px, 100dvh − 352px, 400px)`, so the band fits one screen under the header on any phone and "Looking for more?" fits with it from ~750px tall; the frame's 240 read too short in a browser — plus "Copy Address" and "Yandex Maps ↗" chips in the Interaction rule's chip style, Golos Medium 13) → **"Looking for more?"** with a Visit-page link. The desktop's social CTAs, address bar and form are hidden on phones; the three phone blocks are hidden on desktop. Bar state now applies to every phone CTA on the page, not only the drawer's.

**Address bar wraps instead of breaking (Sep 2026) — a desktop-side fix.** `.contacts-map__bar` takes `flex-wrap: wrap` and a 12px gap at the base, so the chips drop to a second row only when they and the address genuinely stop fitting. It was curing a real overflow: between **992 and 1095** the contacts split leaves the map card just 429–459px against a row needing 434 plus padding, so at 1000 the address was clipped by the Copy chip and Directions ran **37px past the card edge**. Wrapping derives the threshold from the content rather than adding another hand-picked breakpoint — worth preferring wherever a fixed component width meets a fluid column. With the gap counted the wrap now fires across **995–1130**, slightly wider than the overflow it cures, and the bar is one row again from 1135. Re-scanned 985–1220 in 5px steps: no overflow at any width.

**Open:**

- **Striping — settled (Sep 2026):** the build keeps the desktop alternation (Events Soft Peppercorn, Contacts Peppercorn) with the directions band on the *other* ground (Paper by day, Soft Peppercorn by night); the mobile frames (About and Events both Peppercorn, Contacts Soft Peppercorn) are the ones to re-sync.
- **Map provider.** Phones show the same Google My Maps embed as desktop; the Visit page geo-detects Yandex for RU. The "Yandex Maps" chip links out regardless. Decide whether the home map should geo-detect too (one class on the container).
- **Chip size — settled (Sep 2026):** Golos Medium 13 (the Interaction rule's chip style) in the frame's 100px pill with 8/16 padding; the frame's 14px was an undocumented paragraph style and 16 would be too heavy on a phone chip. Re-sync the frame.
- **Day colours** are derived (no day frame): sub-headings Olive, lead Soft Peppercorn, link Olive.

## Mobile — Footer (built Sep 2026, every page)

From `footer-mobile` 1245:39505, as drawn, on the same markup as the desktop footer. Lime block at 24/16/20 with 20px between rows and 0.5px Ash rules: a top row with the **24px Symbol** (the §4 floor) beside a Molot H2 "SWEET PEPPER BAR" in Chili and a **"Top" link** (13px, replaces the desktop bottom bar's Back to top) → **GO TO and HOURS side by side** (Footer → Mobile: two columns; hours labels stack above their values) → the **contacts row full width**: phone (semibold) over the address (caption) on the left, the two **44px social targets** on the right — no stamp, no email, no column title. Bottom row **Lime-light** (`--lime-light` is now a real token, #D4E671; five files had been using it with a fallback) with the copyright centred at Caption size; 34px + safe-area below. **VK now leads Instagram in the social row at every width** (News/social feed → chips out to VK (leading)); the desktop mockup had Instagram first — that is the one to correct. Column titles keep H3 18 (the eyebrow decision; the frame draws 16). Also fixed on the way: the footer's top rugged edge on the home page now matches Contacts (Parchment / Peppercorn) instead of the menu page's grounds.

## Mobile — About page (built Sep 2026)

From `About-mobile-opt2` 1490:78249, as drawn, in one CSS pass (`about.css` → Phones) plus a connector part (`template-parts/about/connector.php` — since Sep 2026 a thin wrapper over the shared `template-parts/components/connector.php`, which Visit uses too with `set => 'visit'`; the About calls and CSS class names are unchanged). Same 48px rhythm as the other phone sections: reflection → 48 → content → 48 → connector; sections drop their own padding on the side a connector carries.

- **Connectors, decided (author, Sep 2026):** the About page takes the home-page device at every seam — nine words (Better together · Word of mouth · Little things matter · Back to the first pour · In good company · The usual suspects · Room for one more · Make yourself at home · Your turn), the word at a section's foot and its reflection opening the next. They replaced the earlier "Tap the card or scroll down ↓" line under the hero grid, which was an instruction in button typography (Interaction rule) and the same call the home hero made when `opt3-word` beat `opt3-link`. The set lives in `assets/sectionLinks/about/` as **desktop-width exports (~1080 wide) of the Figma `SectionLink` component**, colours baked per ground — About is a fixed composition, so one file per word/state serves day and night (the bar-menu precedent). **Both breakpoints, decided (author, Sep 2026):** the same nine seams on desktop, 80px rhythm, so About matches home and menu. *Why the exports were redone:* the first set was the `SectionLinkMobile` component at 370 wide, and it read too bright on phones — same colours as home, but 42px Molot with a 1px outside stroke where the home files are 122px Molot with the same 1px stroke scaled down to a third; measured, the phone set carried ~2.3× the ink of a home connector at 370. Desktop exports scaled down restore parity (ink within a few percent of home at both grounds). Reflection opacity follows **the ground it opens**, not `data-theme` (About never themes): 50% over a light section, 30% over a dark one on desktop, and full over a dark one on phones — the home rules, keyed to the section instead of the hour. *Re-measured in the browser (Sep 2026), all seventeen connectors at 1440 and 402, day and night forced: every reflection reads 50 over Parchment/Paper and 30 over Peppercorn/Soft Peppercorn on desktop, 100 over dark on phones; every foot word 100; night changes nothing on About. The home page under the same probe: 50 / 30 by hour on desktop, 100 by night on phones — the same numbers, keyed to the clock. The phone Figma frames draw `SectionLinkMobile` at 30 (reflection) and 50 with a 0.5px Mushroom rule (word) — the component the SVG rule did not adopt, so the build and the frames differ by design there.* The exports live in Figma on the Desktop page in two utility frames, *About connectors (desktop exports)* (instances, editable) and *About connectors (flattened for export)* (the text nodes the SVGs came from; delete and re-flatten after any edit). Rules touched: WORD OF MOUTH doubled as the connector and the eyebrow under it, so the eyebrow became **PEOPLE SPEAK** — a connector never repeats the eyebrow of the section it opens. Still owed: the nine judged as a set (Voice), the register-collapse check at phone width (a 370-wide connector sits near H1; the seam cut is what keeps it out of the state vocabulary), and RU twins.
- **Header:** the fixed Peppercorn bar (Top nav → Mobile header → About and Visit on phones), page ground Peppercorn under it.
- **Hero:** eyebrow → H1 (36) → lead (16), one 16px gap between the three (the lead's own paragraph margins are zeroed on phones — they were doubling the spacing; the frame draws 8 between eyebrow and title, 16 is the author's call) → 36 → a 2 + 3 grid of the five index cards (3:2 over 1:1, 12px gap — equal widths per row because the cards are peers with no state, unlike the home bento) → **48 to the connector**, as the other phone heroes (home: 36 + the connector's 12); bottom-anchored in nav + 100dvh, checked by the author at 375×667 and 430×932 (a 24px column gap would sit better on the SE — noted, not built); the desktop's 50%-dimmed rest state and dark pill become full-strength photo + Lemon caption label at rest, since touch has no hover to lift them. **Pressed state unified at every width** to the shared card recipe (Lime 1px + `inset 2px -2px 8px` at 25%, no radius change); the hero's own 24px inset and 4→8px radius jump are gone, the hover rules are gated behind `(hover: hover)`, and the inset shadow sits on the overlay because the photo covers the card's own. *Desktop still rests the cards dimmed — open: bring it to the phone rest state, since "dimmed" means inactive and none of the five is.*
- **Concept:** copy on Peppercorn, the picker on a Soft Peppercorn band bleeding across the gutters, the CTA below at full width (dish-picker's phone rules carry over from the pairing station).
- **How it feels — cloud over one quote (decided Sep 2026).** First pass dropped the cloud on phones (it and a three-card rail couldn't share a screen, so the one-clock coupling would have been invisible); the author brought it back with the rail cut to **one card**. The cloud is the row layout (the field's phone fallback) as **wrapping rows, centred, at a phone size trio of 40 / 26 / 18** — still three fixed sizes, hue = tier, outline → fill — about 300px tall for the 16 words; the viewport shows exactly one card, **sized by JS to the tallest quote** (the three-card viewport was cut to the first three cards and clipped the taller ones as the rail turned — the cropping the author saw). Cloud + card ≈ 500px, so the coupling stays in one view down to a 375×667 phone. **Touch grammar:** a tap fills the word, dims the others to 30% and swaps the quote; the heartbeat fills the next word and swaps the quote and un-dims; the 8 s seizure applies. Nothing moves on phones (no centring). The card still swipes (scroll-snap), but the cropped-neighbour "many more" signal is gone — the cloud is the control now. Tier sizes come from the template as an inline `--cloud-size`; phones override through `--cloud-size-phone` (an inline custom property beats any selector). Cost kept in view: the `about-page-copy.md` desktop sizes (64 / 40 / 26 → 56 / 36 / 24 in the build) now have a third trio; one rule in the copy doc, not three. **Revised the same day:** the phone cloud is the field, not rows — the orbit was the thing the author wanted kept, so phones get the same placement and drift (details under Part 3 → Cloud layouts B and E → Shared); rows remain the no-JS state. And hue came off the tier at both widths (Part 3).
- **Perks — revised Sep 2026 against frame 1490:78394.** 3 × 2 stamps in 120px cells (the frame's 124 pitch), the dashed ring shrunk to 72 with the word under it, **circle and word tilting as one** (±4°, the phone frame leans even stamps right where the desktop leans them left — followed as drawn), active straightens. **Phone words** are the copy doc's stamp words (Wi-Fi · Kids · Dogs · Terrace · Your Way · No Stairs): the desktop labels wrapped and made row two taller; desktop keeps its long labels for now (open below). **Colours re-dealt per row** so no column repeats (row two starts on Paprika, as drawn) — a second inline custom property, since the inline desktop colour beats any selector. 36 from the title, **24 to the one-liner** (it is the stamps' caption, so it sits closer than the 36 rhythm). **At every width:** the icons are the Figma `Icons` set inlined from `assets/icons/` and coloured by the stamp (the Phosphor Fill glyphs read differently — a wedge for Wi-Fi, a dim sun); hover now straightens the stamp halfway (4° → 2°) instead of every stamp jumping to −3°; and a press scales the stamp down to 0.94 — the tappable rule's pressed state, which was missing. The active glow stays as drawn (both Figma sets carry it) although `about-page-copy.md` says "no glow, per the flat-effects rule" — conflict left to the author.
- **Story:** one column in the frame's order (title → founder → ¶1 → timeline → ¶2, ¶3 → ledger). **Timeline → segmented year control** (2009 · 2014 · **2026**), one milestone shown; the control *is* the state — filled segment = current, starting on STILL HERE (the frame draws 2014 selected; the grammar says the current year fills), a tap makes another year current. Axis, heat and ticks don't survive the switch — that closes the "does the heat line survive" question in The Pepper Story → Open for phones. **Ledger** keeps the ticket *edge* (scallops in the ground colour above, the card colour below), number left / label right, ruled rows. **Checked against frame 1490:78397 (Sep 2026):** the founder photo is square again (the `<img>` height attribute was pinning 436px once the column got narrower — `height: auto`, all widths); the year control is the frame's tray — Parchment tray, Paper segments with no rules between, Chili + Lemon for the current year, no card border; the ticket tilts 1° like the quote card, its header carries a Mushroom rule, rows are Molot 24 / Golos 14 on Avocado rules, bottom-aligned. **Count-up never fired on short phones:** the entrance watched the whole section at a 35% threshold, and a 2000px section never shows 35% of itself in a 667px viewport, so the counters sat at 0; it now keys off the timeline (half in view) and the clock off the ledger. **Author's pass on the same day:** the title flows as one line where it fits ("THE PEPPER STORY" on every phone from 375 up; the component's forced break was leaving STORY alone) — STORY stays Paprika, the two-line convention's colour, where the frame paints it Chili; the naming sentences ("The name says the whole idea out loud…") now open ¶2 so on phones they follow the timeline card, as drawn (three paragraphs kept; copy doc updated); the founder card's quote mark → text gap is 16 on phones; the ledger header keeps the words (TWELVE YEARS — the author's read: words set the header apart from the digit rows below, the frame's "12" would not); the frame's "make it delicious" typo is fixed in Figma, in the `timelineMobile` component and its seven instances.
- **Guests** 2 × 4 — **checked against frame 1490:78481 (Sep 2026):** description → CTA is the frame's 24 (the shared phone header's 48 was the wide gap the author saw); the title flows as one line where it fits, as Story. **Caption inset stays 16** (Image caption label → Geometry) — the author's gut said 12 because two columns shrink the photos, but measured against the 167px cards two labels ("Pepper's 12th Bday!" 149px, "Pepper's 9th Bday!" 143px) overrun a 135px inner width at 16 and would still overrun 143 at 12; the fix was two shorter labels, not the inset — "12th Bday!" / "9th Bday!" (author, Sep 2026); every pill now clears the inset down to 375. **Team — rows, not a second rail (author, Sep 2026).** The first pass stacked two horizontal rails (wall drift at 240, then the eight cards as a snap rail) and the author found the double rail awkward; a 2-column card grid had been tried and rejected (167px cards too narrow for the card's anatomy). Four layouts were weighed: (1) team as a vertical list of row cards with the fact as an accordion, wall rail kept; (2) the Guests grid borrowed — photo tiles with the name as caption label — rejected because a Guests tile is a link out and a Team tile a state switch, and the same look can't carry two grammars (Interaction rule); (3) one rail with a wall photo above each pair of cards; (4) the wall interleaved vertically between rows. **Built: (1).** Row card = 88px square photo, name / role / chip, 12px padding, 12px between rows, the card's shadow kept; the chip opens the member's line as a reveal under the row (grid-rows 0fr → 1fr, 300ms flat), the card turning Paper as the desktop's active face does; `aria-expanded` on the chip, and the chip gains a pressed state at every width (`:active`, the hover lift — it had hover only); members without a line show no chip on phones (a chip that does nothing is a false door — desktop still shows it, open); all eight carry a line for now — seven are placeholders (about-page-copy.md → Dream Team) — so the layout can be judged with every row open-able. Kept by the author (Sep 2026) knowing the column runs long (eight rows ≈ 1075px); Figma to follow. **Re-decided against frame 2411:70295 (author, 17 Sep 2026):** the Figma row card swaps the face instead (role and chip give way to "← NAME SAYS" + the line, exit on a 16px arrow); the build's reveal stays — the chip is both entry and exit, which is the Interaction rule's only legal toggle here (a bare arrow is none of the three tappable styles, a tap-anywhere close is a fourth), and 16px is no touch target. So the chip now carries an open state on phones: chevron turns to point down (transform on the spring), border and text hold Paprika while `aria-expanded` is true (flat curve). A 2-column photo grid with a span-2 active card (frame 1321:45327) was rejected as well — the Guests-tile grammar again, and the active card reordered the team. *Regression found and fixed the same day:* the tablet pass's `max-width: 260px` on the card reached phones (the phone block set `width: 100%` and never cleared the cap), so rows ran 260 wide at 430 and the chips were clipped — fault 1 of the five, now `max-width: none` in the phone block. Figma still draws the swap; the file follows the build. The wall drift stays the page's one horizontal rail: a clothesline is horizontal by nature. The mobile frame still draws the two rails — Figma to follow the build; **Careers — checked against frame 1490:78518 (Sep 2026):** built as drawn — 48 → header → 48 → three 240px cards at 24 → 48 → CTA text → 16 → full-width secondary button → 48, measured to the pixel in headless Chrome at 402. The description clamps to two lines as the frame draws it (the card is a teaser; the posting lives on hh.ru) — *open: on a phone the card is the only place the guest reads the job before leaving the site; letting the two-line clamp go on phones is the alternative, not built.* The meta line ("Full-time · 2/2") keeps Golos 500 12 where the frame sets Geist Medium — a stray font in the instance, not a decision; 12 is below the 13 Caption floor at both widths, open. The empty state has no phone frame: it takes the position card's 32 inset and a full-width button, as every other phone CTA on the page (it had 24 and a 200px button). **Location — checked against frame 1490:78527 (Sep 2026):** the headline was running three lines on About phones (AT THE VERY HEART / OF THE / BEST CITY) — the About template lacked the menu template's middle "of the" span that `location.css` moves to the Paprika line on phones; two lines now, as drawn. Copy → map is the frame's 48 (the menu page's own frame draws 24, and keeps it); with the button kept it reads as the Guests' CTA → grid 48. The frame's lead is the menu-page instance ("Find us on Kirova Street 10 — … pedestrian Arbat"); the build keeps the copy doc's ("We're on … pedestrian "Arbat,"") — one lead for both pages, open. **Entrance photo:** the frame's `img` carries the caption label ("Main floor entrance", top-left) — built at both widths (24 / 16 inset: the label is a property of the image, not of the breakpoint), wording as the frame's placeholder; the menu page's entrance still has none although its Figma note says pillTop on — open. *Desktop, same day (author):* the Location row stretches — the map runs from the eyebrow's top to the button's bottom (429 at 1440) instead of hanging at the top of a taller copy column; 400 stays the floor, so the menu page, whose copy is shorter, is unchanged. **Visit CTA — re-checked against frame 1490:78533 (author's revision, Sep 2026):** the headline drops from Display 64 to **H1 36** (1.05 / 4%) and the body from 20 to 16 on phones — one line of Lime Molot over four lines of copy, then 40 to the two full-width buttons at 24, closing at 48 / 34 as the menu page's phone closer does. It is now the same size as every section title on the page; what marks it as the closer is the Lime, the missing eyebrow and the button pair, not scale (desktop keeps Display 76 / body 20).

**Open (mobile About):**
- ~~Desktop connector exports~~ — done; both breakpoints carry the nine seams.
- **Page rendered 453px wide on a phone (found Sep 2026, fixed).** The "Write to the team" dialog is `position: fixed`, 558 wide and centred; hidden or not, a fixed element that pokes past the viewport edge makes WebKit grow the *layout* viewport to its right edge (Chromium doesn't), so the header, the cloud and every full-width block laid out at 453 on a 375 phone. Capped at `calc(100vw − 32px)`. Rule to carry: **no fixed or absolute element may be wider than the viewport at any breakpoint, hidden included** — `#page { overflow-x: clip }` only protects the flow. The dialog itself still has no phone design (its footer row overflows at 326 wide); it is desktop-first like the contact form was, and wants the bottom-sheet treatment the reserve drawer got.
- ~~Hero anchoring on tall phones~~ — judged in the browser at both ends (SE, 15 Pro Max): bottom-anchored stays; the home hero keeps its centred block. Two heroes, two rules, both deliberate.
- Location: the frame draws no "Get directions" button and a "FIND THE PEPPER" eyebrow; the build keeps the button (a map with no route out is a dead end) and the desktop eyebrow.
- 🔶 **Live-text connector prototype (Sep 2026) — the SVG rule under test.** The Figma `SectionLink` set (212:2442) rebuilt as HTML text: `template-parts/components/section-link.php`, the `.section-link` block in `components.css`, `section-link.js` (fits each word's font size to the container after fonts load, so the width rule holds without per-word exports), and the About ground mapping in `about.css` → Connectors → live text. Switched on by `$live_text = true` in `components/connector.php` (moved there Sep 2026 when Visit adopted the device — one flag now decides SVG-vs-text for **both** fixed-composition pages, which is the point: the rule should not land on one page and not the other); flip it to compare against the SVGs. Built to the component's numbers, not the site's: Molot 75% / 4%, word at **50%** with a 0.5px Mushroom rule (the site never dimmed the word), dark reflection 30%, light reflection **100%**; strokes Cream 1 / Paper 1 / Olive 2 / Lime 1, the Peppercorn fade in the dark counters (invisible on a flat ground, as in Figma). Known deltas to judge: CSS `text-stroke` is centred where Figma's is outside (a Figma 1px reads ~0.5px outside here); the stroke stays 1px at every size, so phones run ~3× the weight of the scaled SVGs (the SectionLinkMobile look the phone SVG set was rejected for); the Mushroom rule now sits under every word, dark and light. Wording comes from the connector's `alt`. If it stays, the Width-matching rule and "Must be SVG" need rewriting and the home and menu pages need the same part.
- 🔶 **Light-ground reflections are near-invisible — a colour problem, not an export problem (found Sep 2026).** The author re-exported the whole About set from the desktop component; rasterised and compared, the new files carry the same ink as the old within a few percent (the set was re-installed under the template's stems, `desktop/` folder and the two stale files removed). The asymmetry is in the component: on a dark ground the reflection is Paper (17:1 at full, 2.6:1 at the desktop 30%); on a light ground it is **Lime on Parchment — 1.29:1 at full, 1.15:1 at the 50% the rule applies**, and a hairline at that contrast cannot be dimmed into visibility. Same colour on the home and menu pages by day (`atSweetPepper-top` etc. are Lime too), so this is site-wide. Candidates measured on Parchment at 50% / 100%: Olive (the word's own colour) 1.8 / 3.7; Avocado 1.6 / 2.8; Dark Olive 3.0 / 12.8; Lime stays 1.15 / 1.3. Prototyped in the browser with a CSS darken stand-in: **Olive** reads as the word's ghost and lands at 3.7:1 on phones where the rule runs full — the recommended recolour of the `state=reflection, theme=light` variant, then re-export the four About light reflections (wordOfMouth, backToTheFirstPour, theUsualSuspects, makeYourselfAtHome) and the home/menu day sets. Dark Olive at 50% is the closer numeric mirror of the dark side but reads as ink, not a reflection. Open — author's pick.
- 🔶 **Visit CTA label — "Get your table", under consideration (author, Sep 2026).** Switched from "Reserve a table" at both widths on the author's gut that it sits closer to the brand tone (guest-perspective voice: the guest gets a table, the venue reserves one). Not settled: the Reserve tab, the drawer and the home hero still say *Reserve*, so the same action now carries two verbs across the site — decide one at the final review, then align the others.
- **YOUR TURN is 1.5–2.3× the other connectors (author, Sep 2026).** The width rule makes letter size a function of phrase length: the nine exports render at 64–97px bands at 1120 (MAKE YOURSELF AT HOME 64 … WORD OF MOUTH 97) and YOUR TURN at 147. Measured in Molot at 4% tracking, a phrase lands mid-set at 17–18 characters (ROOM FOR ONE MORE, THE USUAL SUSPECTS) and at the set's top at 13–15 (WORD OF MOUTH, BETTER TOGETHER): IT'S YOUR TURN (14) ≈ 102, NOW IT'S YOUR TURN / SEE YOU AT THE BAR (18) ≈ 76, YOUR TURN TO DROP BY (20) ≈ 66, THE REST IS UP TO YOU (21) ≈ 66. **Picked: NOW IT'S YOUR TURN** (author, Sep 2026), wired at the Location foot. The first export was the `SectionLinkMobile` component (367 wide): at 1120 its outline rendered ~3× the weight of the eight desktop exports, and at 370 it was the phone set rejected above — the phone component cannot serve desktop, there is no CSS route round it. Re-exported desktop-width the same day (viewBox 1093 × 76): 78px band at 1120, 26 at 370, Olive at the light-ground weight. Both utility frames on the Desktop page now carry a `NowItsYourTurn` entry (instance and text node, Molot 102.4, stroke 2 outside, fitted to the 1092 content width) so the set stays complete; no `-reflection` twin is needed — the Visit CTA has no head connector. `itsYourTurn.svg` and `yourTurn.svg` in `assets/sectionLinks/about/` are now unused.
- The live headline reads "SINCE 2018" — that is the ACF `about_hero_headline` value in the local database, not the template (fallback is 2014).
- ~~Connectors by night~~ — checked Sep 2026: the About files are one set per ground (Cream word / Paper reflection on dark, Olive word / Lime reflection on light), so night can only break them if a ground moves. One did: the shared Location section followed `location.css`'s night rule to Soft Peppercorn, putting the Lime reflection and the Olive "Your turn" on dark. Fixed in `about.css` — on About, Location and the entrance band are pinned to their day colours by night (Paper / Parchment ground, Avocado eyebrow, Dark Olive body, Peppercorn entrance), per the fixed-composition rule. Still leaking from the same rule: the shared section eyebrow turns Cream by night on About's dark sections (Lime by day) — one token, open.
- Perks labels: one stamp word at both widths (copy doc) or the desktop frame's longer labels; Molot or Golos; Pets (mobile frame) vs Dogs (copy doc, built). And the active glow vs the copy doc's "no glow" — see the Perks paragraph above.

## Tablet — About page (768–991, built Sep 2026)

About had **one** breakpoint before this pass — a single `≤767` block — so the band ran the desktop layout whole. Unlike the home page, which was merely awkward at these widths, About was losing content.

**The page's signature fault is a fixed pixel column inside a fluid one.** Four instances, and naming them together is the point, because each one failed differently and only the first two looked like bugs:

| | fixed value | what it did in the band |
|---|---|---|
| `.about-team__card` | `width: 260px` | 4 of 8 people clipped away at 768–834, 2 up to 1152 |
| `.about-story__founder` | `flex: 0 0 448px` | narrative squeezed to 240px, ~30 characters a line |
| `.location__map` | `width: 600px` | copy column down to 88px; the headline ran 18 lines, one word per line |
| `.about-how-it-feels__quotes` | `flex: 0 0 540px` | 540 is a *width* on desktop; stacked it became a **height**, pinning ~200px of content in a 540px column |

The last one is the one to remember: **a flex-basis changes which axis it governs when the container's direction flips.** Nothing about the declaration looks wrong when you read it.

### Two fixes that belong at every width, not to the band

- **The team card's inactive face was hidden by clipping, not by hiding.** Both faces sat in normal flow; the second was invisible only because the card is `height: 392px; overflow: hidden`. It was therefore `display: flex`, `visibility: visible`, not `aria-hidden` and not `inert`, one card-height below the first — so a screen reader read all eight hidden messages and keyboard Tab landed on a button nobody could see (8 phantom tab stops → 0). It also made a fluid card width impossible, which is how it surfaced. `.is-active` turns the face back on; the phone block re-declares `display: grid` for its accordion, so phones are untouched.
- **The card is now `width: 100%; max-width: 260px`.** A fixed width inside `repeat(4, 1fr)` cannot shrink — the grid stayed 4 × 260 + 3 × 24 = 1112 whatever the column was, and `#page { overflow-x: clip }` swallowed the remainder with no scrollbar. Both of these were live on **desktop**; neither had been noticed.

### What the band does, section by section

- **Hero** — the phone's 2 + 3 bento. Five cards in one row left them ~137px, narrower than their own Lemon caption pills; the phone grid gives **362–446 wide / 237–293 narrow** across the band, against 205 for all five on desktop.
- **Concept** — the phone's rows. `__inner` and `__text` dissolve with `display: contents` and the pieces order themselves. The picker keeps its bleeding Soft Peppercorn band, bled with **the same `clamp()` `.container` uses** rather than the phone's fixed `-16px`, so it lands on the gutter at every width in the band (verified edge-to-edge at 768 / 834 / 991).
- **How it feels** — cloud above the card, as phones stack it; side by side the cloud got 148–315px against the 532 it is placed for and the words collided. **One quote card, not three** (author, Sep 2026): `how-it-feels.js` carries `ONE_CARD_BP = 992`, deliberately separate from `MOBILE_BP`, because `MOBILE_BP` also gates the cloud's drift amplitude and its centre-on-commit — the band keeps the desktop cloud and changes only how many quotes are in view, since the cloud now sits above the rail rather than beside it.
- **Perks** — 3 × 2. Six fixed 120px stamps at 28px gaps need 860px and the band has 736–903, so the sixth dropped alone onto a second row. Six divides evenly by three, which is also the phone frame's arrangement.
- **Story** — the phone layout, **moved rather than copied**: the whole Story run was lifted out of the `≤767` block into its own `@media (max-width: 991px)`, so there is one copy to maintain instead of two that drift. One rule in that run was shared with Guests (`.section-headline-group`); it was split and the Guests half stayed behind, so Guests is unchanged. The band therefore gets the frame's order — title → founder → ¶1 → year control → ¶2, ¶3 → ledger — with the segmented control replacing the heat-line axis and the ledger on the ticket's vertical rows.
- **Team — two columns of the photo card (author, Sep 2026).** Eight members divide evenly into two rows of four and raggedly into three, which left a hole at the foot of the section; three-up was the first proposal and the even division is what decided it. Cards run 356 × 487 at 768 to 440 × 571 at 991, against the designed 260 × 392 — squarer, and about 940px more page than a three-up. Bought because faces are the point of this section.
- **Guests** — unchanged, 4-up at 166–208px.
- **Careers and Perks copy** — two `white-space: nowrap` desktop assumptions removed. The careers meta was cut mid-word in a 229–285px card; the perks one-liner is an intrinsic 677px and painted **139px past the viewport at 768**, 82 at 834, 24 at 900 — the sentence simply ran off the right edge. Phones already set both to `normal`. The perks one only surfaced once the team overflow stopped masking it in the audit.
- **Location** — stacked, with the phone's two-line title split. The map takes **`height`, not `min-height`** — see below.
- **Entrance** — **21:9**, the phones' value and the next ratio up the sanctioned list (Image ratios). The desktop 4:1 flattened to a 184–226px strip here; 21:9 gives 315–387.
- **Visit CTA** — the two buttons split the row with `flex: 1 1 0`, the same idiom as `.section-ctas .btn` and `.hero-ctas .btn`, so all three CTA pairs in the band behave alike.

### Two mechanics worth carrying to other pages

- **`height: 100%` needs a *definite* parent height.** The map iframe is `height: 100%`; on an auto-height box that does not resolve and the iframe falls back to the **HTML default of 150px**, so the map painted a 150px strip inside a 500px well. Desktop escapes it because the row's `align-items: stretch` makes the height definite — a column does not, and `min-height` never does. The phone block had used a flat `height` all along. Found by the author; the tablet build had reintroduced it.
- **A rotation's vertical overhang is a function of width.** The quote card sits 1° off true, and `sizeViewport()` measured `offsetHeight`, which ignores transforms — so it reserved the same room for a 364px phone card (3px overhang) as for an 897px one in the band (8px), against the viewport's fixed 4px padding, and the tilted top-right and bottom-left corners clipped. Fixed in the measurement, not the padding: `getBoundingClientRect()` returns the rotated box, so the room scales by itself. Padding would have needed a different value at every width and would have fought the border-box maths.

### Cost and opens

The band runs **13,985–15,125px** against 10,577 on desktop — the largest stacking cost on the site, on its longest page. Accepted, as on the home page.

- **The founder photo is now full width in the band** — **724 × 724** at 768, 891 × 891 at 991 (12px inside the column: its wrapper carries the `padding-right: 12px` that lets the quote card overlap it). Desktop runs 436. It is the phone treatment scaled up and reads as a poster; capping it around 560 flush left is a one-line change if that is too much.
- The Concept, How it feels and Story layouts here are **the phone frames widened, not drawn**. Nothing in Figma covers 768–991 for this page; the numbers were settled in the browser.

## Tablet — Visit page (768–991, built Sep 2026)

Unlike About, Visit already had a `≤991` block written with tablets in mind — its own comment reads *"Tablets: badges over the map, the map at the full width"* — and the audit found **no clipping and no starved text column** at any tablet width. So the strategy here was not "adapt the phone layout" but "find the handful of desktop rules that stop being true", and the page needed far less than About did.

### What the band does

- **Hero** — the frame takes `flex: 1 0 auto` (was `flex: none` in the first pass). It is `flex: 1 1 0; min-height: 0; overflow: clip` on desktop, so content taller than the flex space was silently cut instead of pushing the hero taller: the contacts card lost **166px at 768, 167 at 834, 169 at 900, 171 at 991** — the email row's caption and everything under it. `none` stopped the clipping but let the foot word float mid-section on any viewport taller than the stack; `1 0 auto` grows to the 100vh floor and never shrinks below content. The hero message also drops the desktop `gap: 48px`, which stacked becomes 48px of air between a headline and its own lead. **The two cards are a centred 80% column (author, Sep 2026) — a recorded break of Grid → Short copy ("never centered").** Three layouts were built and measured before it: the full-width stack (ticket and card 720–943 wide; the contact rows ran the trailing action too far from its handle and a receipt-proportioned slip became a banner), a capped ticket flush left ("feels wrong" — a small left-aligned object in a wide hero), and the desktop row kept down to 768 with the phone card skin (336 / 321 at 768 — one screen, but both columns too narrow). 80% of the tablet column — 577 at 768, 608 at 820, 711 at 991 — was the author's compromise between rows too spacious and columns too narrow; the title block and the running rail stay on the gutter, so the cards are visibly a centred column against a left-aligned headline. The percentage is authored against the tablet column, not the 1120 one, so it scales with the band rather than against it. **The phone rail replaces the desktop band across the whole 768–991 band.** The desktop band exists to be half-covered — at 1280 the contacts card sits on its right half — and wherever the overlap goes (below 1120) that half is exposed as empty Lime. A static band with a lead word and two pills hard on the left gutter fought the centred column; the rail — no lead, no gutters, the pair running edge to edge — softens it (author). Its tilt margin became width-based, `0.8727vw` (width × sin 1° ÷ 2: 3.5px at 402, the Figma value; 6.7 at 768; 8.6 at 991). Rail → ticket runs 60px in the band against the phone's 16 — the stack keeps the desktop 36px drop under the card's pin plus the content's 24px top padding — accepted. 992–1119 keeps the desktop band and the full-width stack. **The hero's gutter is the site ramp now**, not a fixed 24: the hero message, band and content sat 8px right of the Location headline at 768 and 20px left of it at 991. *Found on the way:* the ≤1119 block came after the phone block in source order and its `transform: none` was cancelling the phone card's documented 1° rest tilt (Mobile — Contacts); the reset is scoped `768–1119` now and phones rest at 1° again.
- **Location** — badges run full width under the map, two-up (356 / 381 / 440 cells, no name truncating). That matches what the phone block has always done — `order: 2` plus a two-column grid — so the band follows it rather than inventing a third arrangement. *Second pass:* the list had kept its desktop `flex: 0 0 360px`, a width in the row that became a **360px height** in the stacked container (Responsive faults, 2). Chrome stretched the three grid rows to fill it — 121 / 103 / 103 — and Safari left them at natural height and showed the remainder as a gap under the grid; one fault, two browsers, two symptoms. `flex: none`, and **every cell now takes the tallest row's height** (`grid-auto-rows: 1fr`, 60px — the active badge's, with its hint line), so switching routes never re-flows the grid and no cell is stretched by its neighbour; the map container dropped from 824 to 675. Columns are `minmax(0, 1fr)`, as on phones, so a long name can ellipsise instead of widening a column.
- **Good-to-know slip** — full width on the section ground. **The on-map placement was built and rejected** (author, Sep 2026): it sat on the embed's own controls, and — the part that settles it — the scalloped edge is a **cut-out in the section ground**, so over map tiles it rendered as Parchment blobs. A component whose edge is a hole in the background cannot be moved onto a background it does not own. Full width also needed the internal `space-between` to go: right in a 348px card where label and note nearly touch, it stranded the divider mid-card across 736–903.
- **Entrance photo** — the `≤767` 21:9 band (`img` 1441:72476) now runs to 991: **768 × 329 / 834 × 357 / 991 × 425**, and the closure's inline 3:2 photo steps aside as it does on phones. The CTA's head connector goes with it — the rule moved from `≤767` to `≤991` rather than being duplicated, because *the band carries the seam and the reflection goes* is a consequence of the band, not of the width.
- **CTA** — the button pair splits the row (`flex: 1 1 0`), matching `.section-ctas .btn`, `.hero-ctas .btn` and `.about-visit-cta__buttons .btn`. And `.visit-cta__body` had no margin reset, so the browser's default `1em` on a `<p>` sat on top of `.visit-cta__text`'s `gap: 24px` — the body ran 40px from the headline where the rest of the column ran 24. Fixed at the base rule; it was wrong at every width.

### Two bounds that are derived, not chosen

- **The badge column is pinned, the map flexes.** The fixed width moved *from* the map (`736px`) *to* the badges (`360px`). The badge column is the one with a hard content requirement — its names must not ellipsise — while a map is legible at any size, so the column that cannot give is the one that is pinned. With the map pinned instead, the badges were squeezed to 144–300 between 992 and 1279 and **five of the six names truncated**; now none does at any width, and 1280 is unchanged at 360 + 24 + 736 = 1120.
- **The hero split stacks below 1120.** Its two columns are `flex: 0 0 548px` and `width: 524px` — 548 + 48 + 524 = 1120, exactly the design column, so it only ever fitted at 1280 and the contacts card hung **213px past the viewport at 992**. Both columns here *can* give, so both shrink from their own basis; the bases sum to the 1072 available at 1280, keeping 548 / 524 exact. That fixed the overflow but not the second fault: the card is pulled up over the state band by the header's −40px margin, and as the columns shrink its left edge walks left across the chips. Rectangle-intersection testing shows it covers "KITCHEN'S CLOSED" from 992 (61 × 25px) to 1119 (19 × 25) and is clear from **exactly 1120**. Per-component bound, like the home page's section header at 1152; it does not move the page's band, since the location and CTA splits are fluid from 992. *The bound is the overlap's, not the row's: keeping the row below 1120 without the overlap was built and measured (see What the band does → Hero) and rejected on width, so the stack stands.*

### Copy

Sovetskaya's hint is **"Past the fountains"** — the build had drifted to the longer "Across Andropova, past the fountains"; the shorter line is what `visit-page-copy.md` already specifies. Bogoyavlenskaya's hint is restored to **"And the old Kremlin"** (author, Sep 2026), overruling `visit-copy-review-en.md` line 112, which had recommended omitting it as "incomplete"; that note should be marked settled rather than left contradicting the build.

🔶 **"Sovetskaya sq." — a consistency call, not a constraint.** Measured in the 360 badge column, where the name cell is 172px: "Bogoyavlenskaya Square" needs **195** and genuinely does not fit, so its "sq." is a necessity; "Sovetskaya Square" needs only **148** and fits with room, so its "sq." is there to match. That makes a workaround into house style by imitation — the original abbreviation is recorded as *"stays until the author settles it"*. If both should read in full, a **384px** badge column clears the 195 and neither needs abbreviating, trading the Figma 360 for correct copy.

## Tablet — Menu page (768–991, built Sep 2026)

**The band takes the phone menu page whole, widened rather than copied** (author, Sep 2026: *single section across the whole band is just simpler*). One reading model below 992 and one above; rotating an iPad to landscape (1024+) changes the chrome and the columns, not how the page is read. This is the one page that does not follow Grid → Tablet's "phone chrome over desktop content", and the measurements are why.

**Measured before the pass**, desktop layout under phone chrome: the hero nav is a fixed 440, so the photo stack was **182px at 768** (249 at 834) beside a 200px description; the deal is a fixed 352 in a title-row slot of 226–283 and ran **59px into the "Tea & Coffee" headline** at 768; the two dish columns were **344–428, narrower than the 370 a phone gives the same rows**; the jump tab (44 deep, hidden only below 768) sat **28px over the rows** at the 16px gutter, while the Reserve tab was already hidden at ≤ 991; and a tablet has no hover, so the side-nav's one word carried both preview and commit — the touch exception's case. Page length was not the problem (13,656 / 16,159 against desktop's 13,791), so the two-column, all-sections layout was a live option; **the derived split for it is 838** (two 370 columns + 48 = 788 of content, reached at 838) and was not taken. Single-section: 6,371 / 5,855 at 768.

- **Hero** — the phone hero across the band; its fixed 16px gutters are now `--gutter` (variables.css — the same ramp `.container` uses, so the stack lines up with the section below at every width). **The photo runs 21:9 in the band** — a recorded exception to Image ratios (21:9 is "never used for content cards", and this photo is one: the preview and the link). Judged in the browser against the alternatives: 3:2 at the full column is 736 × 490 at 768 and **895 × 597 at 991, where the hero overruns one viewport by 68px** (the home page's stacked-photo open, made worse); 4:1 turns the Manhattan into a rim; 16:9 is the excluded middle. 21:9 fits one viewport at every width (728 × 312 → 895 × 384) with 73–120px of slack above the opener, which the centred block absorbs. A flush-left cap at ~560 was rejected by the author. **Each hero photo carries a focal point** — `focus` in `inc/menu-sections.php`, CSS `object-position`, set inline by PHP and by `menu-hero.js` on every swap. Only the vertical value matters: the crop keeps 64% of a 3:2 photo's height, so `50% 30%` keeps the top (Soups' bowl), `50% 70%` the bottom (Kids' plate). Phones and desktop stay 3:2 and ignore it.
- **Jump-nav** — the edge tab hides at ≤ 991, the Reserve tab's bound (menu-jump-nav.css). The panel stays the desktop 448px drawer, opened from the hero's button; its words preview the hero and the active word drops its arrow, as on phones — `menu-jump-nav.js` gates on 991 now.
- **Sections** — one at a time (`menu-single-section.js` at 991), the rail sticky under the 76px header, words at 36, the current word at the gutter (`alignRail()` reads the rail's own padding instead of a hard 16). The top block bleeds with `--gutter`; its 7:3 photo *is* 21:9 (the same fraction), 329 tall at 768, 425 at 991. Subsections in one column at 736–903. **The deal at full width with its content flush left** — the desktop `space-between` was drawn for a 348 card and stranded the text at the far end of a 736 coupon.
- **Highlights — 3 × 2 grid** (author), 24px gap, cards 229 at 768 → 285 at 991, at or above the desktop rail's 262 for most of the band; desktop link labels. The PHP's alternating tilt classes make a checkerboard by themselves in a three-wide row, so only the phone's 2-up keeps the explicit pattern.
- **Pairing station** — the phone rhythm (the tail reflection flush at the top, 48 / 24) and **the phone's stacked result** (author, Sep 2026): the desktop row was built first — 344 + 344 at 768 — and rejected as cramped, and because the 48px between photos and ticket had no right value against 16–44px page margins. Photo pair full width above the ticket (364 squares at 768, 447 at 991), ticket with 8px radii and the 48px top inset, and **the phone's colours with it** — Olive "x", Parchment night ticket (author: colours follow the layout, not the device). Tags keep the desktop wrap (two rows); only the phone's tag rail stays below 768. The section no longer fits one screen (1,091 tall at 768) — accepted on a tablet.
- **Entrance photo, Location** — already the shared 768–991 block (location.css). The phone-only "Come sit with us" closer stays phone-only; the band ends on Location and the footer, as desktop does.

**Accepted for now (author, Sep 2026):** the deal at full width flush left; 992–1119, where the desktop poster still runs with a 441px photo at 1024 (only a narrowed desktop window hits it).

**Open (tablet menu):**

- **Tablet type styles — the author's next job.** The band mixes display sizes: rail words at 36, the Highlights and picker titles at H1 64 (Highlights wraps to two lines at 768). Typography has Desktop and Mobile modes only; the author is setting up the tablet paragraph styles (a tablet mode in the Figma `Typography` collection), and the build follows once they exist.
- **Focal points — final review.** The hero values in `inc/menu-sections.php` were chosen from a contact sheet, not on a device; the section-header photos have `--img-pos` wired but unset, so their 21:9 top-block crops are centred. Both wait for the final review pass.
- **Night connectors** run at the desktop's 30% in the band (phones go to 100% because the scaled stroke reads grey) — check at 736–903.

## Mobile — Menu page (built Sep 2026, layout and structure)

From `menu-browser-day` 2109:128382, as drawn; components (deal, add-ons, highlight card, picker ticket, dish rows) still render their desktop anatomy squeezed to 370 and get their own pass next. Both states × both themes verified in headless Chrome at 402.

**Hero** (`menu-one-photo-hero-kitchen-day-stacked-mobile` 2109:130201). **One viewport, as the home hero (decided Sep 2026):** nav (76, fixed) + hero = 100dvh; the opener and the content travel as one block centred in the slack, the connector sits on the floor; short phones grow (375×667 overruns by ~23px, content-driven). The side-nav panel has no room on a phone, so the hero is one column: a **flush-left Lime button** ("More plates" / "More pours" — see Open) with the author's **kebab glyph** (`assets/icons/kebab.svg`; the build inlines a `c-kebab.svg` currentColor twin so it takes the button's Peppercorn instead of the file's Lemon) that opens the jump-nav panel, the photo stack at the full 370 (sheets fanned into an 8px bottom-right pocket, photo level; **two fan recipes alternate by section** as the components draw — Salads, Soups, Desserts, Beer and No Buzz put Chili mid-slot peeking bottom-left, Lemon at the back), the description, and a **full-width primary that commits** to the section on show. **Its label and glyph are per section (Sep 2026)** from the Figma `menuFood` / `menuBar` variables as recorded in `menu-hero-copy.md` (`ctaLabel`, `Icon`; the author's refinements there win — Salads, not "Salad Bar"), so the button reads "Sandwiches & Bagels" with the burger, "For Kids" with the fork, "No Buzz" with the glass; the glyphs are the file's own `Icons` set, inlined as currentColor twins (`assets/icons/c-*.svg`). Captions and descriptions come from the same source; the Cocktails photo is now the Manhattan (`manhattan-3-2.jpg`, caption "Manhattan"), No Buzz the berry smoothie (`smoothie-1.jpg`) and Infusions the cranberry glasses (`infusions-lenya-11.jpg`, caption "Berry festival" kept for now), in the hero and in their section headers (dedicated 4:1 crops for Cocktails and Infusions — see Image ratios → Section-header crops). Bar hero description is **Parchment** (`menu-bar-mobile` 2175:68888) — at every width since Sep 2026 (desktop night used to keep Mushroom). **Section eyebrows reviewed (Sep 2026):** Kids "favorites they finish", Lunch "weekdays 12pm – 4pm" (pm for the EN audience; a documented exception to the clockless numeral style), Desserts "always room for something sweet", Infusions "Home-made, since 2014"; the other twelve already matched the Figma `Subtitle` values. The room wordmark gives way to the **"food menu" / "drinks menu" connector** at the hero's foot (the existing desktop SVGs, `foodMenu` / `drinksMenu` + their `-reflection` twins, first use) and its reflection opens the next section on the next section's ground. *Rule check:* the wordmark rule says "always filled"; on phones it is replaced, not outlined, so the rule stands — the connector is a connector (Section connectors → scale register).

**Jump-nav on phones — the drawer previews, the button commits (decided Sep 2026).** The left-edge tab is hidden below 768px — a phone edge has no margin for it (the Reserve-tab finding, Top nav → Mobile). The panel opens only from the hero button and is built from `kitchen-drawer-day` 1341:49297: the sheet fills the viewport with the **Lime edge on the left** (8px, top-left corner rounded 4), padding 48 / 72 / 48 / 48, the word list at 16 and the door 48 below, centred; close × at the frame's spot in a 40px target. **Picking a word swaps the hero** (photo, caption, description, and the commit button's label and anchor) and closes the panel — no scroll, no hash. That restores the touch exception's two channels exactly: the drawer word is the preview, the full-width button is the commit, and tapping the already-shown word is never a dead end because the button is still there. On desktop the same panel still jumps. **The door does not preview (decided Sep 2026):** on phones it stays a link to the other menu page — it carries an arrow, and previewing a bar section on the food page would show a hero for a list that isn't on the page; this also means the component's `drinksCoffee` state has no route and isn't built. Consequence applied: the **active word drops its trailing arrow on phones** — settled: a filled word, no arrow, no glyph (a per-meal glyph on the active word was tried and didn't help) (the arrow means "this word is the action"; the frame draws one — re-sync). The panel opens showing the hero's section, not the section in view. Between sections the rail below does the jumping.

**Sections — one at a time (author's prototype, built Sep 2026)** (`menu-food-mobile-day` 2109:130225). On phones the page shows **a single menu section**; the other eight (six on the bar side) stay in the DOM — server-rendered, one URL, deep-linkable — but are hidden until chosen. The section on show follows the hero's section (a drawer word previews the hero and the page follows, no scroll), the **rail** in the section headline, the highlight-card links and the URL hash; each switch writes the hash (`replaceState`) and, when it came from a link, rides the house spring to the section top under the header. Reason: nine stacked sections made the phone page ~17,500px; it is now ~6,000 (bar: 22,000 → 4,700). Consequences applied: every section now sits between the same neighbours, so the striping is fixed (body on the page ground, top block on the surface) instead of positional; the **nine per-section connector words give way to one** — "try the match maker" — after whichever section is on show, with its reflection opening the picker, as the frame draws. Desktop is untouched (all sections, positional striping, per-section connectors).

One column. Each section opens with a **top block on the surface ground** (`--section-top-bg`, so the block reads as the seam under Highlights or the hero reflection): the section photo full-bleed at 7:3 (radius 4, caption inset 16) → the **rail** → the eyebrow, closing with 8px bottom radii. Then, on the page ground: the deal at full width, the subsections as one column at 24px, the tail connector. Section rhythm 36px to the connector (Figma 24 + 12).

**The rail replaces the headline — and is the page's navigation on phones.** The section's `<h2>` sits in place inside a horizontal snap rail of all the section words in **canonical order** (Figma `carousel-food` 1409:50231): current word filled (the headline's own colour), the rest outlined in the hero nav's strokes (Olive by day, Lime by night) — outline → fill, the one state vocabulary, at reading size, so it stays inside the state register. One component at both widths (`menu-section-rail.php`): on desktop only the `<h2>` renders. **Position:** the rail is scrolled so the current word sits at the 16px gutter — earlier sections are reachable by scrolling left, the next one peeks at the right edge (Lunch: Breakfast at −202, Lunch at 16, the frame's own numbers); the last words (Desserts, Kids) sit where the rail's end lets them. *(Replaces the build's earlier wrap-around order, which made the previous section unreachable — author, Sep 2026.)* **Sticky — decided Sep 2026:** the rail sticks under the fixed header while its section scrolls; the eyebrow scrolls away. Same element, no new device; it answers the discoverability and dead-zone concerns — a one-section page needs a tab bar and this is it. Cost accepted: 54px under the 76px header. A tap on a stuck rail switches the section and lands on its top. Words are plain same-page anchors; on phones `menu-single-section.js` switches the section instead of scrolling the page.

**Highlights** (2109:130202): six cards in a **2-up grid** (16px gaps) instead of the rail, title → 24 → grid → 48 → connector, the hero connector's reflection above. Tilts form the frame's checkerboard (+1 −1 / −1 +1 / +1 −1), not the rail's simple alternation. **Night** (menuSection 2194:70367, checked Sep 2026): cards Peppercorn on the Soft Peppercorn section, Cream titles, Lime links, Cream eyebrow — as the build already had; the one addition is the title's second line **"Highlights" in Paprika on phones by night** (the day frame keeps one Chili title; desktop unchanged). Ground: Soft Peppercorn, as the desktop night frame (SeasonalMenuSection 833:34450) draws it — author confirmed Sep 2026; the phone night frame's Peppercorn is the one to re-sync, like the day frame's Paper. Eyebrow **Avocado at every width** (Sep 2026) — the menu page's own eyebrow colour and what both frames of this section draw; it had inherited the home page's Olive from the shared header component. *Ground — decided Sep 2026:* Parchment (the desktop striping), so the connector + reflection sit on a ground change; the frame's Paper is the one to re-sync. Card links take a phone-only short label ("In Soups" — `link_label_mobile`) because "Show in Desserts →" does not fit a 145px column. Titles run one line with an ellipsis as the frame draws two of them.

**Pairing station** (2109:130226, picker `foodPairingContainer-mobile-day` / `-night` 1432:54562 / 1432:53793 — layout only; the content is the desktop's). The reflection of the tail connector sits flush at the section top, 48 to the header, 24 to the picker, 48 to the **"you'll like it" connector, which ends the section** with the entrance photo directly below (the current frame shows it; an earlier one hid it). Tags become a scroll rail from the left viewport edge with **"or Shake It!" pinned beside it at the right** (decided Sep 2026: at the end of the scroll the Shake chip sat at x≈540 on a 402 phone — invisible on load and after every pick, and the random pick is the half of the picker that earns its keep on touch); the tags fade out into the gap before "or", so the cut tag reads as "more". The picked tag scrolls to the gutter on load, after a pick and after a Shake (re-measured once Golos has loaded — the fallback face is narrower). Row height unchanged (28px), so the whole picker still fits one phone screen: header to ticket foot is ~620px. Wrapping the tags into rows was rejected for that reason — it would push the ticket below the fold. *The frame still draws Shake inside the scroll; re-sync.* **Status: kept (author, Sep 2026), with a refinement pass still owed** — the tag scroller is ~240px wide beside the pinned chip, one tag and a sliver at a time; judge on a real phone whether the fade, the widths or the row itself need another look. the two photos stay side by side with an **Olive** "x" by day (Cream by night; the desktop's Chili stays desktop); the ticket goes full width with **8px radii and a 48px top inset** — and is **Parchment by night on phones** where the desktop ticket is Cream (both as drawn; one of the two to reconcile if the ticket should be one colour everywhere). The night component's own header ("try it yourself", H2 Lime) is not used — the section keeps "find your match!".

**Entrance photo + Location + closer** (2109:130245 / 130246 / 130264, built Sep 2026): the entrance photo full-bleed at 21:9 with the `img` component's 4px radii; **Location on Parchment** (the desktop's Paper stays desktop; Soft Peppercorn by night) at 48 / 34, the copy over a 400px map at 24, and the headline breaking after "heart" — Chili "at the very heart", Paprika "of the best city" — where the desktop breaks after "of the" (one span swaps side per breakpoint). Then the **phone-only "Come sit with us" closer** on Paper (Peppercorn by night): eyebrow "join the party", H1, the About page's visit copy, and one full-width Reserve button (`primary-green`, phone glyph trailing) that opens the reserve sheet — the frame's earlier two-button version is gone. The desktop menu page has no such section; it is phone-only by design until the desktop says otherwise.

**Desktop side effect, deliberate:** the Kids section's connector is now **"try the match maker"** (was "favorites they'll finish") — it is the one food section whose connector sits on the picker rather than the next section's photo, so it names what follows. Still non-interactive (Section connectors); if it should *link*, that is a rule change to argue first.

**Open (mobile menu):**

- **Opener label and glyph — settled (Sep 2026):** "More plates" / "More pours", kebab glyph. Arrows were tried and read as page navigation; the room glyphs stay on the commit button, where they name the section. The bar component (2175:68888) still reads "More plates" — re-sync. RU twins still owed.
- **Rail discoverability — settled (Sep 2026): the sticky rail.** With one section on show the rail is the main navigation, not a shortcut, so it stays under the header while the section scrolls (author confirmed in the browser). The floating "More plates" alternative is rejected: it collides with the CTAs at the foot of sections (the Reserve finding) and its drawer previews the *hero*, so mid-page it would change something far above the guest or need a second behaviour.
- **Tail connector gap:** 44px above the word today (the frame's 36 plus the subsection's 8px bottom inset), 1px onto the picker. Author to set the final number in the browser; the other phone sections run 48 to their connectors.
- **Commit button label — settled (Sep 2026): "Breakfast"**, as drawn. The connector under the hero already says "food menu", so the button names the section, not the document.
- **Bar connector word — settled (Sep 2026):** "drinks menu" (the `drinksMenu` SVG); doors name contents and the food twin is "food menu". The bar component's "bar menu" is the one to re-sync.
- **Bar hero description colour — settled (Sep 2026): Parchment at every width**, the bar component's value; the desktop rule moved into the drinks-state block (menu-hero.css). Kitchen night keeps Mushroom.
- **Drawer word colour.** The frame outlines the words in Chili (Word list roles → "Day: Chili outline") with Kids in green; the build keeps the desktop panel's Olive strokes (Lime by night), which is also what the hero nav ships with. One of the two is stale — the hero, the panel and the rail should take the same answer.
- **Visit CTA — built phone-only (Sep 2026).** Whether the desktop menu page should end the same way is still the author's call.
- **Highlight titles:** the frame wraps two titles to two lines and truncates others; the build truncates all. Component pass.
- **Highlight card arrow — decided Sep 2026 (author confirmed): keep →.** The card link jumps to a menu section on this page, and every other section jump here — hero words, drawer words, the rail — uses →; the arrow marks "this is the action", not a compass. The one ↓ in the house (Visit → Directions) is a single link pointing at the map directly beneath it. Six ↓ in a grid would read as a download list and would split one action across two glyphs on one page. The wrong-version test: dropping the arrows and making the whole card the link fails on touch — nothing would mark the card as tappable.

## The no-clock rule (hospitality principle)

Good service makes guests lose track of time — so no literal clocks or countdowns in the hero. Schedule is implied by slider position and soft copy ("the kitchen's on — no rush"). Exact times appear only where guest anxiety is real (breakfast cutoff may say "served till noon") and on service pages (menu, contacts).

## The bartender's ticket

A site-wide component: an order slip the interface "prints" to answer the guest — the kitchen–bar handshake made tangible. Motif lineage: the bottle-cap scalloped edge (`design.md` §5.1).

- **Anatomy:** Paper card · small-caps header line (SWEET PEPPER · KIROVA 10, Caption tracking) · the guest's line (Golos Medium, Peppercorn) · dashed rule · the bar's reply (Golos, Deep Chili) · optional chip exit link · bottom edge cut with the bottle-cap scallop.
- **Motion:** it *prints* — slides out from the control that produced it, with the house spring easing. Never fades in like a tooltip.
- **Modes:** always Paper, day and night — a receipt is paper regardless of the hour. On dark grounds it's the brightest object in the section, so max one per view.
- **Voice:** the reply is always a person speaking ("unforgettable with a shot of the cranberry infusion"), never a template ("pairs well with X").
- **Used in:** About — the pairing picker prints one (spec: `about-page-copy.md`, The Concept). Candidates later: reservation confirmation, menu specials.
- **Not the footer any more (Aug 2026).** The footer became a saturated Lime band in both modes (Section striping), and Lime out-brights Paper — a slip there loses the "brightest object in view" property the component depends on. Hours, address and "come sit with us" live in the footer as plain type instead. One closing device per page.

## Motion language

One vocabulary across the site, so every interactive section feels like one hand:

- **Outline → fill is the state change.** Slider meal labels, the About chapter rail, the word cloud, the Story timeline's markers, the heat-filling wordmark — "filled" always means active / current / hot. Photographic surfaces state it as **dimmed → full strength**: the mobile hero's daypart tiles unlit the three that aren't selected rather than haloing the one that is (Mobile — home hero). Same grammar, applied to images instead of letterforms.
- **One clock per section.** A single idle rhythm drives everything that moves in a view (About's heartbeat fills a word and turns the quote wheel together). Never two independent idle animations in one section.
- **Idle self-demo, human seizure.** Untouched sections demo themselves (heartbeat, an occasional shake); any interaction seizes the clock and pauses auto-motion for ~8s, then it resumes. **Two sections stop for good instead (documented exceptions):** the menu hero's idle sweep (Menu page → Idle behaviour) and, since Sep 2026, **How it feels** — the first touch on a word, a card or the rail ends the heartbeat for the visit, because a guest who has just discovered the interaction and is exploring it must not be interrupted by the demo (author). The 8 s pause there times only the visual release (undim, ease home). Heartbeat 5 s, was 3.5.
- **Scroll tweens take the spring too.** A `scrollTop` animation can't use a CSS easing, so `how-it-feels.js` solves `--ease-gentle` (cubic-bezier 0.33, 0.57, 0.08, 1.19) in JS and rolls the quote rail over 900 ms with it, snap off during the tween — native `scroll-behavior: smooth` was the browser's own curve at its own speed and read harsh. Reuse `gentleEase()` for any future scroll roll.
- **Spring easing** for prints, rolls, and snap-backs (bartender's ticket print, wheel roll); transforms only — never animate layout. *Exception recorded Sep 2026 (author to confirm or narrow): the mobile hero tiles animate `width` / `height` during the tap-to-move FLIP. A transform-only move with counter-scaling would stretch the photo crops mid-flight; a size transition keeps `object-fit: cover` honest. Position still moves on `transform`.* (The build also uses Gentle site-wide, wider than "prints, rolls and snap-backs" — see `report.md` → Motion.)
- **Colour-only transitions take the flat curve, at the house duration.** Background, border and text colour move on `--ease-gentle-flat` over `--dur-gentle`; the springy `--ease-gentle` overshoots past the end value on a colour and flickers. *Swept Sep 2026:* the address chips (`.contacts-chip`, home Contacts + the Visit map bar) were the last holdout on a hand-written `all 0.15s ease` — also an `all`, which animated layout properties nothing intended to move. Chips, buttons and the Visit route badges now share one declaration.
- **prefers-reduced-motion:** drift, parallax, and auto-advance off; state changes (fill, quote swap, ticket content) remain.

## Grid

Full-width/full-bleed sections use a 1120px content width (centered on the page, 80px side margin at the 1280 design width), split into a 12-column grid with a 24px gutter (column ≈ 71.33px — re-checked Sep 2026: 12 × 71.33 + 11 × 24 = 1120 exactly).

**The side gutter ramps; it does not step (revised Sep 2026).** One declaration carries every width, and it replaces the old 16 → 24 → 80 steps:

```css
padding-inline: clamp(16px, 12.5vw - 80px, 80px);
```

`12.5vw − 80px` is one-eighth of the viewport less 80, which passes through exactly **16px at 768** and exactly **80px at 1280**, so both authored endpoints survive and the phone gutter below 768 is untouched. In the ramping band the content column is **0.75 × viewport + 160** (verified against the browser at nine widths).

It replaces a hard 24 → 80 step at 992 that made the content column *narrow as the window got wider* — 943 at 991, 832 at 992 — dragging everything sized off it backwards: hero tile 186 → 162, cards 298 → 261, connector SVG 943 → 832, and the section headline from two lines to three. A second, smaller step at 767/768 (735 → 720) went with it. `vw` rather than `%` because one `.container` on the menu page sits inside `.menu-visit-cta`, where a percentage would resolve against the wrong box; the cost is that `vw` counts the scrollbar (~1.6px on the gutter).

### Spacing — the 4px rule (documented Aug 2026, from the Figma file)

**Every spacing value is a multiple of 4.** That is the whole rule; there is no separate named-token vocabulary to learn, and nothing needs a name like `space-md`.

- **Spacing scale (Figma `Spacing` collection):** 4 · 8 · 12 · 16 · 24 · 36 · 44 · 52 · 64 · 80
- **Icon sizes (Figma `Sizes` → `Icons`):** xs 8 · s 12 · m 16 · l 24 · xl 36 · xxl 48

Both are subsets of the 4px grid, not separate systems. Use a scale value where one fits; any other multiple of 4 is legal when it doesn't. The now-marker's 8px dot (The day/night concept) is on this grid, as is the 24px column gutter above.

**Carve-out — the page gutter interpolates (Sep 2026).** The ramped side gutter above computes to off-grid values between its endpoints (20 · 33 · 44 · 60 · 70). The *authored* values are still 16 and 80, both on the grid; what the clamp produces between them is interpolation, exactly like a fluid type scale. The rule governs gaps that things stack and align against, and nothing aligns against a page margin — the 24px column gutter, every internal gap and every spacing token are unaffected. A `round(down, …, 4px)` variant was built and rejected: quantising keeps every computed gutter on the grid, but it turns the content width into a sawtooth that climbs ~37px then drops 8px at each step, trading one 111px cliff for fourteen 8px ones.

*Note for the Figma file, not for the build:* spacing and icon sizes are each modelled as **one variable with many modes** rather than many variables. It works, but modes are a theming axis — a frame sits in one spacing mode at a time, so nested elements needing different gaps need nested overrides. No effect on the CSS, which just gets the scale above.

**Body copy is capped by column count, not by character or word count.** A live word-count-driven rule renders inconsistently depending on the actual copy dropped in; a fixed column span stays predictable regardless of content.

**Desktop: body copy caps by column count** inside full-width sections, even when the section itself is full-bleed — never the full 12. Narrower breakpoints get their own column rule; don't assume the desktop span holds below desktop (Tablet, below, now defines it).

> **Open — this entry and the build disagree, and always have (found Sep 2026).** The doc says **6 of 12 columns (548px)**. `.section-description` in `components.css` says **643px**, which is exactly **7 of 12** (7 × 71.33 + 6 × 24 = 643.33). Both are grid-legal and nobody has been reading the doc's number, so this is a naming fix, not a layout bug — pick one and make the other match; `components.css` is the single place it lives. Its sibling `.section-title-block` = **739px** checks out as exactly **8 of 12** (8 × 71.33 + 7 × 24 = 738.67) and is correct as authored.

**Mobile (defined Sep 2026):** the Figma frame is 402 wide with **16px gutters → 370 content**; body copy runs the full 370. The hero bento is **3 equal columns with a 12px gap** (wide slot = 2 columns + gap ≈ 242, narrow ≈ 115), everything else is single-column. In code: the nav collapses at **≤ 991px**, phone layouts and the 16px gutter apply at **≤ 767px**; the 4px rule holds throughout (see the carve-out above for the page gutter alone). The drawer uses 24px gutters (its own frame).

The leftover columns get a job, not left blank — which one depends on how much copy there is:
- **Short copy** (2–4 sentence teasers, e.g. News cards): flush-left the text to the same grid start as the headline above it, and pair the remaining columns with a secondary element — a stat, a pull-quote, or social icons. Never centered — centering a narrow column in a wide section reads as an accident, not a choice.
- **Long-form copy** (blog posts, About page body, full menu item descriptions): split into two columns of running text side by side instead of one narrow column plus empty space — the standard editorial fix for reconciling full width with a readable measure.

### Tablet (768–991, built Sep 2026 — with the section header running to 1151)

No separate tablet composition was drawn and none is needed: **tablet is phone chrome over desktop content.** The nav collapses to the drawer at ≤ 991 while the body keeps the desktop grid, and the ramped gutter above removes the need for a tablet container of its own.

**The band has two bounds, deliberately.** Chrome flips at **991** (the nav, the drawer, the hidden Reserve tab). The section header flips at **1152**, because that is where its two halves actually fit — see below. One number was chosen for the chrome and the other derived from the content, and they are not meant to match; a rule that reaches past 991 is not a bug unless it has no arithmetic behind it. What the band does need is the handful of places where a desktop rule stops being true, or where a phone rule was labelled 991 by accident. Nothing here was drawn in Figma first; it was measured in the browser and the numbers below are from that pass.

**Percentage widths authored against the 1120 column are the recurring fault.** Each holds at the design width and squeezes everywhere below it. Three were found; expect more on the inner pages.

- `.about-preview-text` was `flex: 0 0 55%` — 396–519px in the band, which broke the Molot headline to three lines. **`width: 100%` cannot override it:** on the main axis flex-basis wins. Stacking the parent then re-caps the block at the description's 643, because `align-items: flex-start` makes a column item shrink-to-fit. Two independent caps, and which one binds depends on whether the parent is a row or a column — check both before concluding a width is unsettable.
- `.section-title-block` was `width: 66%` — the fluid equivalent of 739 only at a 1120 column, and 549 at an 832 one. Now `min(100%, 739px)`: the authored Figma number, correct at every width.
- `.section-ctas .btn` is a fixed **261px** inside a slot only 221px wide at 768. Right-aligned, so the overflow pushed left *into the paragraph* — 8px into Highlights, 16px into Contacts, 2px into Contacts still at 834. A real collision, not tightness.

**What stacks.** Both menu previews and the About preview take the phone's single column, photo first, from ≤ 991. The menu page goes further and takes the phone model whole — one section at a time — see Tablet — Menu page for why it is the exception to "phone chrome over desktop content". The `≤767` blocks were **widened rather than copied**, with the phone-only extras (48px section rhythm, 12px stat icon, full-width CTA, 16px caption inset) left behind in a `≤767` block — two near-identical media blocks would have drifted. The argument for stacking rather than keeping the split: at 768–991 the two 50% halves leave the dish list **350–434px, narrower than the 370 a phone gives the same rows.** The About preview follows the phone's ordering exactly (eyebrow → headlines → photo → description → stats → CTA), not photo-last.

**The section header stacks to 1151, not 991 — and the bound is derived, not chosen.** Side by side the header needs 739 (title) + 24 (gap) + 261 (button) = **1024 of content**, and the content column is 0.75 × viewport + 160, so 1024 arrives at exactly **1152**. Below that the two halves do not both fit, and the choice is a squeezed three-line headline or the stack; the headline is the section's identity, so it wins. The CTA slot runs as a **row of equal buttons** there rather than the phone's column of full-width ones — two stacked full-width buttons across 736–1023px read as a form, not as a pair of doors (Interaction rule). Re-derive the bound if the gutter ramp, the 739 or the 261 ever move, and **check it again when Polylang lands: RU labels are longer than EN.**

**Contacts runs full width in the band** (author's call, Sep 2026). The `≤991` block that stacks `.contacts-split` stays as it is — map above form, form across the column. Recorded cost: the form field measure, inputs at 620–843px. Separable from the layout if it ever wants capping. Two map fixes went with it:

- **The address bar stays a row.** It had been stacked here only because the `≤991` block was written for phones — and below 767 the bar is `display: none` anyway (phones use `.contacts-map__chips--phone`), so the stacking rule had no phone to run on. In the band the bar is 734–901 wide against a row needing ~466, so it fits with room and keeps its desktop height, 56 rather than 91.
- **The map is taller, and fluid.** `min-height: clamp(400px, min(60vw, 55vh), 600px)` replaces an inherited phone 300. 600 reads well on a desktop browser narrowed to this width but eats a real tablet screen, so width drives the height — which also keeps the map near 3:2 against the content column — and the `vh` term guards short landscape viewports. Measured: 461 at 768×1024, 500 at 834×1112, 547 at 912×1368, 595 at 991, and 400 at 960×600 where the guard bites. `vh` not `dvh`, so the map does not resize while a mobile toolbar hides.

**Open — the stacked photo.** Full-bleed 3:2 is 786 × 524 at 834 and **903 × 602 at 991**, larger than anything on the desktop page (1280 gives 542 × 375), and it makes the band 23–32% taller than desktop (8,716–9,363px against 7,095). The same rule already runs at 700–767, so it is not new — just far more visible at 903. Two ways out and the docs pull against each other: cap the stacked photo at ~560 flush left and give the leftover columns a job (Short copy, above), or let it take the **4:1** band ratio, which is exactly the divider job 4:1 was added for — except Image ratios says 4:1 and 21:9 are *never used for content cards*. Undecided; resolve before launch.

**The About page has its own entry.** Tablet — About page records that band separately, because About is a fixed composition with eleven sections of its own and the faults there were content-loss, not awkwardness. The general lesson from it is worth keeping here: **a fixed pixel column inside a fluid one is this project's recurring tablet bug**, and it does not always look like one — a `flex-basis` written as a width on desktop silently becomes a height the moment the container flips to a column.

**The home hero in the band — the phone bento, on portrait tablets (author, Sep 2026).** The row was measured first: four **137–174px** squares across a 720–900 column read as thumbnails, the active tile's size advantage was the desktop's weak 15–25%, and the hardcoded 76px headline wrapped to two lines below ~870, so 768 and 820 showed a two-line Display headline over a strip of small photos — the desktop stack narrowed, the same fault as the desktop's empty right third. The phone bento at the same widths: the active photo **473 × 320 at 768, 499 × 335 at 820, 584 × 392 at 991**, narrow tiles 207–256, and the hero still one viewport on every portrait tablet (948 at 768 × 1024, 1104 at 820 × 1180). In practice the 768–991 band *is* portrait tablets — 768 / 810 / 820 / 834, all ≥ 1024 tall; every landscape tablet is 1024+ and takes the desktop row. **Guarded by height, not width:** the bento query is `(min-width: 768px) and (max-width: 991px) and (min-height: 1000px)`, so a short viewport in the band — a landscape phone, a narrowed desktop window — keeps the row; the bento hero is ~1100px tall there and its headline sat 926px down at 991 × 744. **What differs from the phone, in its own block:** the eyebrow and the second CTA return (the phone cut them for space; the pair splits the row), the headline is **64px** — the Typography mobile Display size, not the phone's raw 32; one line for the current copy from 768 — the subhead 20, the grid gap 24 (the grid gutter; the span formula follows), the primary button at its natural width. The language nudge stays hidden as on phones. Colour is Chili in both modes, as on desktop (author, 17 Sep 2026 — the divergence closed the day after it reached three widths). **Costs recorded:** at 768 × 1024 and 991 × 1200 the content is taller than the section's minimum, so the foot word starts 54–67px below the fold (the buttons are above it at every width); the grid-to-headline 32 and the 24px top padding are the slack if the word should sit on the fold. Tap verified at 820: the tile moves into the wide slot on the spring, the theme flips. *Deliberately wrong version considered:* two-sided at tablet — copy left, a 2 × 2 bento right, the desktop's own leading alternative — rejected here because a ~340px copy column runs the 64px headline to three lines and stacks the button pair; worth prototyping at 1120+, where the desktop already wants it. **Single-button CTAs in the band sit left, at natural width (author, 17 September 2026).** The kitchen preview's button had kept its desktop right-alignment (`.menu-preview-cta--right`, the mirrored row's) under a now left-aligned list, 500px from its own text, while the bar preview's and the About preview's sat on the grid start; it is left now, so the three stacked previews agree. Natural width, not full: one full-width button across 736–903 reads as a form field, the same reason the section header's pair stays a row of two (above). Phones keep their full-width CTA. *Wrong version considered:* right-aligning every single CTA as a "read on" exit, the way the Visit ticket's "See what's new →" sits at its right edge — a text-link habit that leaves a button belonging to nothing on a 720–900 column; rejected. **Also noted:** the footer's four columns fit exactly at 768 with the address ending on the container edge, no slack for RU; no horizontal overflow, no clipped nowrap text, section header, events three-up (224–285) and contacts as documented.

### Responsive faults — the five that keep recurring (Sep 2026)

Written after the home, About and Visit tablet passes, because the same handful of faults produced almost every bug in them. Check these before reading a layout as a design problem.

**1. A fixed pixel column inside a fluid one.** The single most common fault — **seven** were found and fixed across the three tablet passes: the team card (260), the About founder column (448), the shared map (600), the About quotes column (540), Visit's map column (736), its hours column (548) and its contacts card (524), plus the home page's 261px CTA button inside a 221px slot. Each holds at the design width and either clips content away or starves the column beside it below that.

*A grep for `width: NNNpx` / `flex: 0 0 NNNpx` finds more that are currently benign — the 356px team wall card (inside the drift rail, which is meant to be wider than the viewport) and the 640px careers empty state (not rendered). Re-check either if it ever lands in normal flow.*

> **The question that resolves it: which column has a hard content requirement?** Pin that one and let the other flex — Visit's badge names must not ellipsise while a map is legible at any size, so the badges are pinned and the map gives. Where **both** can give, give each a `flex-basis` and make the bases sum to the design column, so the design is exact at full width and the pair scales together below it (Visit's hero: `1 1 548px` + `1 1 524px` against a 1072 gap-adjusted column). Where **neither** can, the layout has a derived breakpoint — see 5.

**2. A flex property means something different once the axis flips.** Eight instances — the eighth found a day after this list was written: Visit's badge list kept `flex: 0 0 360px` into the stacked container and became a 360px-tall box, which Chrome and Safari then rendered differently (stretched rows against a gap). `align-items` is vertical alignment in a row and horizontal in a column, so `center` and `flex-end` written for a desktop row silently centre or right-shove every child when the container stacks — Visit's hero message pushed its title block to x=302 against a 24px gutter, and its location header centred the Good-to-know slip with 217px either side. `flex-basis` is worse: it governs width in a row and **height** in a column, so About's quotes column — `flex: 0 0 540px`, a width on desktop — became a 540px-tall box around 200px of content. Nothing about either declaration looks wrong when you read it.

**3. `height: 100%` needs a *definite* parent height.** An iframe with `height: 100%` on an auto-height parent falls back to the **HTML default of 150px** — the Location map painted a 150px strip inside a 500px well. A flex row's `align-items: stretch` makes the height definite, which is why desktop escapes it; a column does not, and `min-height` never does. Use an explicit `height` on the stacked parent.

**4. A transform's overhang scales with the element, not the breakpoint.** A rotation's vertical overhang is a function of **width**: the 1° quote card overhangs ~3px on a 364px phone card and ~8px on an 897px one, against a fixed 4px viewport padding, and the tilted corners clipped. Measure with `getBoundingClientRect()`, which returns the rotated box, rather than padding the container — padding needs a different value at every width.

**5. Derive breakpoints, do not pick them.** Every bound that has survived was arithmetic: the home section header at **1152** (739 + 24 + 261 = 1024 of content, reached at 1152), Visit's hero split at **1120** (where the card stops covering the band), the team grid's fluid card (4 × 260 + 3 × 24 = 1112 never fits a tablet column). Write the arithmetic into the comment so it can be re-derived when the copy, the gap or the component widths move — and **check it again when Polylang lands**, because RU strings are longer than EN.

**6. A boundary between two elements is not a boundary you own.** Two flush surfaces meeting at a fractional y — every section is text-height, so most do — are rasterised separately, and at any non-integer scale (DevTools "fit to window", Windows at 125–150%, a scaled screenshot) a hairline of whatever is *behind* them can show. Overlapping by a pixel only helps when both surfaces are the same colour and the parent behind them is too; the Visit footer's dark hairline was the two Lime footer blocks over the page's Peppercorn body. Two rules make a seam impossible: paint the parent the shared colour (`.site-footer` is Lime now), and where two *different* colours meet, give the boundary to one element as a border plus background painted in one pass (the hours ticket carries a 1px Peppercorn top border; the scallop strip's centre row sits on it). `top: -1px` on a relatively positioned strip is the wrong tool — it moves the paint and leaves the flow box, opening a 1px slot below. Not reproducible at an integer device scale (checked at 2× in headless Chrome), so verify hairlines at 100% before treating them as layout.

**And a measurement note.** Proximity on one axis is not collision. Comparing `band.right` to `card.left` said Visit's contacts card overlapped the state band from 992 to 1175; testing whether the two rectangles actually intersect said 992 to 1119, because above that they are vertically separated. The wrong test produced a plausible, wrong breakpoint.

## Layout notes

- The giant Molot wordmark can sit on the seam between sections: outlined on the dark side, filled crossing into the light side. Replaces the old mirrored-reflection idea.
- Section striping is a deliberate rhythm, but it's not one rule across both states — see Section striping (day / night) below.
- A persistent "Reserve" tab stays on the right edge in all states. On the menu page, past the hero, a matching menu-nav tab holds the left edge (Menu page → Sticky jump-nav).

### Section connectors ("link words")

Documented Aug 2026 — the device existed on the home and menu pages before it had an entry here. Giant outlined Molot set across the full-bleed photo band between sections ("yummy morning", "soul therapy", "the best in the city"). One component, used on both pages.

- **Must be SVG format**: These link words must ALWAYS be exported and placed as SVGs, not rendered as HTML text.
- **Request missing words**: If a particular section requires a link word and you don't have the SVG asset for it, you MUST stop and ask the user to provide it.
- **Width matching**: The width of the SVG link word MUST exactly match the width of the content container (in Figma, this is 80px margins from the viewport). SVGs should be given `width: 100%` inside the standard container. *Mobile (Sep 2026): the container is 370 (402 − 2×16) and the desktop SVG scales to it. By night the scaled SVGs run at full opacity on phones (desktop: 30%) — at a third of their stroke weight, any dimming reads as dark grey on Peppercorn (author, Sep 2026); day keeps 50% at every width. The Figma mobile component (`SectionLinkMobile`) draws live Molot text with a gradient fade instead of the SVG — not adopted; the SVG rule stands until the author says otherwise.*
- **Non-interactive — and that is the specification, not an omission.** A connector is not a link, not a section heading, and not the photo's caption. It must never acquire a hover fill, a trailing arrow, or a block: those are the three tappable grammars (Interaction rule), and borrowing one would make a decorative word claim to be a door or a chip.
- **Why outlined Molot here doesn't dilute outline → fill.** Elsewhere outline means *not current* and filling means *active*. Connectors never fill, so they'd be the one place outline is merely texture. The carve-out that resolves it is **scale register**: outline → fill governs Molot at *reading and interaction* sizes — word lists, slider labels, nav items, chapter rails. Giant display Molot that bleeds off the layout is a separate register where outline is a treatment, not a state. The room wordmark and the seam wordmark (above) already live in that register; connectors join it. Keep the two registers visibly far apart in size — if a connector ever shrinks to near-H1, it re-enters the state vocabulary and the carve-out stops protecting it.
- **Two jobs, one component (known, deliberate).** On the home page a connector is a forward invitation to the section below. On the menu page it supports the section it travels with. The author's call is that the menu-page reading is the better fit for a dense list page.
- **Open — the direction problem.** A word sitting *between* two sections has no inherent referent; position can't tell the guest whether it looks forward or back, so today it's resolved only by reading the words. Fix by pinning the referent structurally rather than semantically: either every connector band belongs to the section **below** it (becoming its epigraph, which also unifies with the home page), or every band belongs to the section **above**. Pick one, move the bands, and rewrite the copy that no longer fits. Until then, expect the odd misread on first visit. *Visit (Sep 2026) adopts the first reading outright: its two connectors — YOUR ROUTE TO PEPPER at the hero's foot, DROP A LITTLE NOTE at Location's foot — each name the section below, and the page is recorded as "a band belongs to the section it opens". If that reading is later chosen site-wide, Visit is already compliant; if the other one is, Visit's two words have to move.*
- **Voice — review them as a set, not one at a time.** Section names are plain and each list gets exactly one voiced name; connectors are a separate register and aren't bound by that count. But eight-plus winks down a single page is a lot of personality per scroll, and the tone only reads as a set. Cut the weakest before launch rather than defending each on its own.
- **Copy lives in Figma only.** The home- and menu-page connector wording isn't in any project file yet — pull it in so it can be voiced in RU as register twins rather than translated late.

## Section striping (day / night)

The dark/light/accent section rhythm runs on different logic in each state (decided in the home-page IA work). Value alternation — not a literal colour swap — is what carries it, so a photographic section and a flat-colour section can share a value without reading as a repeated beat.

**Day — clean alternation.** Sections alternate light/dark down the page (Paper/Parchment ↔ Peppercorn), with a saturated band (Lemon) used sparingly as the one "colour-block moment" (§2.4 allows saturated fills for banners only). One sanctioned adjacency: the Hero and the section directly below it (Highlights) may both sit light — the Hero is photo-with-overlay, Highlights is flat cream, different enough in texture that it doesn't read as monotonous.

**Night — immersive dark.** Night does *not* checkerboard; it commits to darkness and lets Lime/Chili accents glow out of shared dark — "being in the bar after dark" rather than a daytime-poster rhythm. Break up long dark runs with **Soft Peppercorn** `#201E22` (`design.md` §2.2 — a ~4-point luminance lift over Peppercorn: texture without contrast), **not** Dark Olive. Let one bright section (e.g. Lunch) be the single point of light in the middle. Optional night softener: light sections that stay light at night can step Paper→Parchment to shrink the value jump at each seam — not required.

**Contact section — dark in both modes.** The closing zone reads as the wind-down regardless of daypart. At night this is simply consistent with everything around it; in day it's a deliberate exception (it accepts one two-dark run, News→Contact) chosen so Contact always feels like the wind-down zone. **Scope revision (Jul 2026):** this rule covers the *home page's closing section* only. **Corrected Aug 2026:** the Visit page does not theme by time either — the Jul revision assumed it did. Visit and About are **fixed compositions** (The day/night concept → What themes and what doesn't); Visit runs dark · light · dark into the Lime footer, in every state. Its sections don't swap clothes, so the striping rhythm is authored once rather than twice.

**Footer — saturated Lime in both modes (revised Aug 2026).** The footer used to fall under the rule above; it no longer does. It is the one place on the site that **stops theming altogether** — a fixed landmark at the end of every page, in both day and night. That's a more useful rule than a wind-down that changes clothes, and it gives the closing zone a single identity across the whole site.

- **The scalloped edge belongs to the section above (Aug 2026).** The footer's Lime ground never changes, but its scalloped top edge takes the colour of whatever section it meets — Paper, Parchment, Peppercorn or Soft Peppercorn. The seam is the last beat of the section it leaves rather than the first beat of the footer, which is what lets one fixed Lime band sit under every possible ground without a hard collision. The scallop is the bottle-cap motif (`design.md` §5.1); it is **not** a bartender's ticket and must not acquire slip anatomy — see the bullet below.
- **Consequences.** The footer is now the brightest object on any night page, so it can't also hold a bartender's ticket — the slip depends on being the brightest thing in view (`design.md` §5.1, revised to match). One closing device per page, and Lime is it. If the Peppercorn → Lime seam reads harsh at night, step the section directly above the footer to Soft Peppercorn so there's a beat before the green.
- **Text on Lime is constrained.** Peppercorn (12:1) and Ash (5.1:1) only. **Olive fails at 2.86:1** and Chili at 2.65:1 — see `design.md` §2.5, where the old "passes on Lemon/Lime" claim was corrected. Olive is the easy mistake here, since muted-green-on-green looks right and measures wrong.
- **GO TO mirrors the top nav (decided Aug 2026): Home · Menu · About · Visit.** Events is removed — it stays out of every nav, header and footer alike, until its page exists (Top nav; Launch phasing). Mockups carried an Events link in the footer; that is the one to correct, not the rule.
- **Mobile: two columns, not four stacked groups.** Stacking turns a ~350px desktop band into ~1,050px — 1.2 full screens of unbroken saturated green, at which point it stops reading as a band and starts reading as a page. Pairing GO TO with HOURS (both short) and running REACH US full width brings it to roughly 610px, which fits one screen and preserves the desktop proportion. Hours labels stack above their values rather than sitting beside them; that's what makes the two-column split fit at 402px.

## Image ratios

The image component ships six fixed ratios — one per content job, not one per breakpoint:

| Ratio | Use |
|---|---|
| **3:2** | Default for our own art-directed photography — menu items, drinks, hero, staged event photos. Landscape frame leaves room for the environment (bottle wall, bar counter) around the subject, per the photography rules in `design.md` §7. |
| **2:3** | Portrait counterpart to 3:2. Same ratio on every breakpoint — a portrait card doesn't need to get taller on mobile just because the phone screen is taller. |
| **1:1** | Square, general use. |
| **21:9** | Cinematic full-bleed banners only (≈2.35:1 — functionally the same, cleaner CSS fraction). Reserved for wide environment shots — bar counter, terrace, full room — not tight action shots (a pour, a close-up), which get squeezed or cropped out at this ratio. Never used for content cards. *Exception (Sep 2026): the menu hero's photo stack in the tablet band (768–991) runs 21:9 with a focal point per photo — judged in the browser against 3:2, which overran the viewport at 991, and 4:1, which is a divider; see Tablet — Menu page.* |
| **4:1** | Section divider bands on information-dense pages — the menu's per-section hero image (added Aug 2026). Same subject rules as 21:9 (wide environment and dish-in-context shots, never a content card), but chosen where 21:9's height pushes a long list too far down the page: at 4:1 the photograph reads as a divider and the text keeps the lead. Desktop-first; it can carry other band jobs where the same "photo serves the copy" logic applies. |
| **4:5 (Instagram)** | Default for News/social-feed cards specifically. These are reposts/links out to Instagram rather than our own photography, so the ratio follows current creator norm instead of the house system. |

**Deliberately excluded:**
- **16:9** — redundant middle ground between 3:2 and 21:9; no video-embed use case to justify a sixth ratio.
- **9:16** — a genuine full-bleed mobile hero is a `min-height: 100dvh` + `object-fit: cover` problem, not a fixed-ratio-component problem. Social cards link out rather than embedding native reels, so there's no source ratio to preserve either.

For the large/featured News card: split layout (image beside the copy, not stacked full-width above it) rather than a full-bleed banner — keeps 3:2/4:5 photography at a natural crop without needing an even wider ratio just to control card height.

**Two band ratios — settled: both stay (author, Sep 2026).** 21:9 (≈2.33) and 4:1 are both "wide band" ratios, and 16:9 was excluded precisely for being a redundant middle ground. They are far enough apart to justify each other: 21:9 is a cinematic full-bleed moment, 4:1 is a divider that yields to the copy. The earlier note — retire 21:9 if 4:1 ends up doing every band job — is closed: 21:9 now carries the menu hero in the tablet band (Tablet — Menu page) and the author keeps it for future updates regardless.

**Section-header crops — rule of thumb (Sep 2026):** the 4:1 header takes the section photo with a focal point (`--img-pos`) by default; a dedicated `-4-1` crop only when the subject spans the frame vertically and no offset keeps it (Cocktails' Manhattan, Infusions' cranberry glasses). Crops live beside the original in `photos/menu-website/…` with the ratio suffix.

### Image caption label (the Lemon pill)

A named part of the `img` component, not a per-layout decision — a Lemon block naming the subject of the photo. Hidden by default; position (top / bottom) is a component property, chosen per image so the label never lands on the subject. Used site-wide, desktop and mobile.

- **Anatomy:** Lemon `#FFED00` ground, Peppercorn text, **Golos 400** · 13 / 140% / 4% tracking — the Caption style (weight closed Aug 2026 by re-syncing `design.md` §3.3 to the Figma file, where `Body/Caption` is and always was 400). No icon, no trailing arrow, no hover state.
- **Geometry (recorded Sep 2026, from `pill` 224:3148 and `img` 224:3156):** 4px / 8px padding, 4px radius, Lemon at **90% opacity**, **no tilt** — the tilt belongs to the menu page's door and the team wall's photos, never to this label. Inset from the photo edge: **24px on desktop, 16px on phones** (the Figma mobile frames still draw 24 — author's error, the code is right).
- **The colour is sanctioned, not an exception.** Lemon behind Peppercorn text is an approved fill (`design.md` §2.5), and a caption is a short burst rather than an editorial surface (§2.4). Contrast ≈ 16:1.
- **It is not a chip, and must never become one.** A chip is Golos Medium 13 in a container **with an icon and/or trailing arrow and a hover state** (Interaction rule). The caption label deliberately has none of those three, and that absence is the whole reason the third tappable style stays unambiguous. Never add an icon, an arrow, or a hover to this label — if a photo's label needs to be tappable it becomes a chip and stops being this component.
- **Known collision, accepted:** the EN/RU switch is also a text pill and *is* tappable. The two never share a viewport (header/drawer vs. photo) and differ in ground, type and content, but pill-as-a-shape now means two things. Accepted as-is; if a third pill-shaped element appears, resolve the shape properly rather than adding a fourth.
- **Interactive images — the one restriction.** Where the photo is itself a link, a separately-edged bright object sitting inside a tap target it isn't will absorb mis-aimed taps and compete with the real action. There, either use the dark-scrim caption, or keep the label and make the whole card one tap target with the label visibly inert.

## Menu page

Decided in the menu-page work, Jul 2026. One page, `/menu`, holding both menus.

### States vs theme — two independent axes
The page has two **content states** — food (kitchen) and drinks (bar). State is not theme: theme (day/night) follows time of day site-wide; state only *defaults* from it (day → food first, evening → drinks first) and the guest overrides freely. Kitchen content at 9pm is kitchen content in night clothes — content and theme never lock. Every state × theme combination exists, so hero components carry three props: `slide` × `menu (food/drinks)` × `theme (day/night)`. Each state and each section anchor is deep-linkable.

### Hero — side-nav poster (concept B, validated Jul 2026)
Chosen over the carousel/ticker and the split-screen concepts after stakeholder A/B + usability testing: cleaner, easier to navigate, interactions clearer, less information-heavy. All sections visible at once — time-to-dish is the page's KPI.

**Anatomy.**
- **Nav panel (left):** a Parchment sheet with a Lime offset edge, bled off the left viewport edge, top-aligned with the photo stack's top line; its base **merges into the next section's Parchment band** — one continuous surface, so the column visibly belongs to the content it navigates. Holds the section word list and, as its last item, the door.
- **Photo stack (right):** 3:2 photo with a hard Peppercorn keyline on offset Lemon + Chili sheets, each rotated a degree differently, sheets smaller than the photo (placemats, not a container). Depth by colour-block layering — no shadows. Caption is **non-tappable** — either the dark-scrim treatment (Golos Caption on a small dark scrim, bottom corner) or the shared Lemon caption label. Photo card is the link to the active section, so whichever is used, the whole card is one tap target and the caption carries no edge of its own affordance. *(Revised Aug 2026: the original line read "never a pill" — an over-broad ban written before the `img` component's caption label was accounted for. The real risk is a separately-tappable-looking object inside a link, not the pill shape; narrowed accordingly. See Image caption label.)* Night twin: same stack, quiet dressing — Soft Peppercorn sheet + thin Chili edge ("the same table after dark"). If guests read a Polaroid into it, that's a free easter egg — never design toward it (no white frames, no handwriting).
- **Description** below the stack on the page ground (never on a mat), per-state component property. No CTA button **on pointer devices** — the word list's arrows are the actions; a button returns only if a state gets a non-navigation action.
  - **Touch exception (decided Aug 2026) — the no-CTA rule is scoped to pointer input, not retired.** On desktop, hover and click are two separate channels, so one word carries both *preview* (swap the photo) and *commit* (go to the section). Touch has one channel; a word cannot be both. Rather than drop the preview — the mechanic concept B won its test on — touch gives commit its own object: tapping a section word previews it, and a boxed action below the description commits ("Breakfast menu"). *Built on phones Sep 2026 (see Mobile — Menu page): the word list lives in the jump-nav drawer and previews from there; the boxed action is the full-width button under the description, labelled per section from the Figma `ctaLabel` ("Breakfast", "Sandwiches & Bagels", "For Kids"…), not "Breakfast menu" — settled Sep 2026, the connector already says "food menu".* Consequences, all required together: the active word **drops its trailing arrow** at this breakpoint (the arrow means "this word is the action," and on touch it isn't); tapping the **already-active** word commits, so a second tap is never a dead end; the photo card stays a link to the active section, as on desktop.
- **Room wordmark:** giant Molot, bleeding off the corner, always the current room, always filled — colour is `--accent-2` (Olive by day, Lime by night), one rule across modes.

**Word list roles** — colour carries ownership, outline→fill carries state:
- Defaults: outline → active fills + trailing arrow (arrow may be hover/active-only on section words). Day: Chili outline (resolves the old Paprika-on-Lemon check — there is no day band anymore). Night bar state: Paprika outline, active Chili.
- Shared sections: Lime outline → fill Lime (day: Olive). One green only; Lemon never marks a word role.
- Hover on a word swaps the photo — a navigational preview (crossfade, preloaded), not decoration.

**Idle behaviour:** opening frame = daypart answer (kitchen: breakfast morning, lunch midday…; bar: tea & coffee morning → no buzz midday → infusions evening → cocktails late). Then the highlight sweeps in list order, wrapping; first frame holds ~2× the cycle step; any interaction stops the demo **permanently** for the visit (documented exception to the 8s-resume rule — nav must not wander after use). The demo never scrolls, never changes the URL. `prefers-reduced-motion`: no sweep, static daypart frame. **Time proposes only on first entry; session memory disposes after** — a guest's explicit room choice is remembered and never re-overridden by the clock.

**The former ticker** is retired for navigation: the sticky jump-nav is an edge tab + slide-in panel (see Sticky jump-nav below), decided with the section layouts, Jul 2026. The running word-loop survives only as a *display* motif candidate (e.g. an upsell strip) — never as navigation, so scrolling text never means two different things on one page.

**Amended Aug 2026 — scrolling text now has a second job.** The Visit page's mobile status rail runs the bar/kitchen states in a Lime band (`visit-page-copy.md` → Mobile layout). The original wording of this rule ("never means two different things") was written when the ticker's only rival job was navigation; the standing rule is narrower than it reads. State it as: **scrolling text is never navigation, and never carries more than one job per page.** Day menu = Lemon display ticker; Visit mobile = Lime status rail; the two never meet. If a third job appears, resolve the device properly rather than adding a fourth — same discipline as the pill shape (Image caption label).

### Doors — the third tappable style
A door is a Molot word standing on a small tilted block with an always-visible arrow; it means "cross to the other menu." Doors name **contents** (FOOD / DRINKS — "drinks" honestly promises the smoothie); wordmarks name places (KITCHEN / BAR if used). No "MENU" suffix — the page already says menu. Doors are postcards of their destination and never re-theme:

- **Primary door** (last item of the nav word list): the FOOD door is Cream fill + Peppercorn word, hover/focus-visible fills **Lemon**; the DRINKS door is Peppercorn ground + Lime word (outline → fill on hover) — including on the day page. On dark grounds the dark door needs its own separation — Lime hairline or scalloped edge (night override).
- **Secondary door** (in-list shortcut to a section of the other menu, if used): outline grammar — Cream outline frame + word at rest, fills Lemon with Peppercorn word on hover/focus. Secondary = unlit, primary = lit; same button logic as everywhere.
- **Mirroring:** DRINKS exits with →, FOOD returns with ← — direction encodes the room metaphor, keep it site-wide.
- **Motion:** hover nudges 2–3px toward the arrow (spring, transform only); on switch the page crossfades states (candidate payoff: the split-screen seam from concept A sweeping across as the transition). `prefers-reduced-motion`: instant swap. Candidate refinement: the bottle-cap scalloped tear on the block's edge ("tear here to switch menus").
- Touch has no hover: rest state must carry the affordance alone (block + arrow do this).

### Sticky jump-nav — edge tab + panel (decided Jul 2026)
The page is long; once the hero's side-nav has scrolled away, a jump-nav takes over. The compact-ticker candidate lost to this: a loop re-reads the list one word at a time, while the panel restores the whole list at once — same reason concept B won the hero.

- **Trigger:** a persistent tab on the **left** viewport edge — the menu-side twin of the right-edge Reserve tab. Lime ground, vertical label naming the current state ("Food menu" / "Drinks menu") plus a Phosphor icon. Appears only after the guest scrolls past the hero (from the second section on); while the hero side-nav is visible it would duplicate it. Present in **every state × theme combination** — the drinks list is shorter but its sections are just as long, and one behaviour keeps the mechanic learnable. *Phones (≤ 767px): no tab — the hero's "More plates" button opens the same panel, and each section's headline rail does the jumping; see Mobile — Menu page.* Figma can't prototype scroll-gated appearance — this is documented build behaviour, mockups show both states.
- **Panel:** slides in from the left edge (transform only, house spring; `prefers-reduced-motion`: instant swap). Anatomy is a reprise of the hero nav panel — Parchment sheet, Lime offset edge — so it reads as the hero's column coming back, not a new device. Contents follow the existing grammar unchanged: the section word list (outline → fill active, green rule for shared sections) with the **door as its last item** (mirroring rule applies: DRINKS exits →, FOOD returns ←).
- **Dismiss:** close ×, scrim tap, Esc, or choosing a word — picking a word scrolls to its anchor and closes the panel. Focus is trapped while open.
- **Not a fourth tappable style:** the edge tab is the Reserve tab's established pattern; everything inside the panel uses existing word/door styles. Nothing new to sanction.


Nav words map to guest intentions; page structure maps to the kitchen. They need not be 1:1 (a nav word may anchor a sub-head).

- **Food (9):** Breakfast · Lunch · Bar Snacks 🔶 · Salads · Sandwiches · Soups · Hot dishes · Desserts · Kids.
  🔶 **Bar Snacks — working name, under consideration.** Renamed from "Snacks & Boards" for a measure reason (too long for the 4-column nav slot with the arrow) — a legitimate poster-logic edit, but two unresolved costs: it re-demotes the boards (the original objection to "snacks"), and the word "Bar" inside the kitchen list sits near the DRINKS door — possible cross-link misread. Runner-up to test-fit: **To Share** (shorter, elevates boards, registers with «На закуску»). Decide, then propagate in one pass: nav, menu-hero-copy.md, menu-en.md, RU pairing.
- **Drinks (7):** Infusions · Cocktails · Wine · Beer · Spirits · No buzz · Tea & Coffee. (Cross-link to food snacks travels via the shared section / secondary door, not a permanent eighth word.)
- **Voice rule:** section names are plain; each list gets exactly **one** voiced name — «На закуску» on the food side, «Без градуса» / No buzz on the bar side. RU and EN are register twins, not literal translations. No second wink.
- **SUMMER dropped from the nav** (decided Jul 2026): seasonal is a tag, and a tag in a section list over-promises. The season's name lives as the Seasonal rail's own Molot heading ("SUMMER AT PEPPER"), which must sit high enough on the page to do the announcing — if the rail sinks, reopen the nav-word question.
- **Soups stays a separate section** (tested Jul 2026): participants *could* find soups under Hot dishes, but agreed it doesn't belong there. Print still files it under hot dishes — an accepted, documented web↔print divergence; keep the item data mapped so generate-print still works.

### Kitchen structure
Course order is canonical (matches print — taxonomy stays identical across web and print for the future generate-print-from-WordPress option); dayparts are a navigation lens on top, never a filter that hides content. Merges vs print: sandwiches + bagels = one section; sauces fold in as an appendix under grill/hot (no anchor of their own); soups are a full section on the web (see Section labels — tested decision) while print keeps them inside hot dishes. Kids: last section in the food list with a `#kids` anchor — no separate page (thin page, and a separate surface is where the old kids sub-style drifted off-brand; kids styling stays inside the three ramps).

### Shared sections — the green rule
Two sections belong to both rooms, one in each direction: the **soft bar** (No buzz + Tea & Coffee) is owned by the bar, rendered in both states; **Bar Snacks** 🔶 (working name — see Section labels) is owned by the kitchen, rendered in both states (it's what people order with drinks). The green ramp marks sharedness on three coordinated levels: the ticker word (Lime/Olive), a value-stepped section ground (Soft Peppercorn at night — assign night's texture-break sections to the shared ones, so the striping rhythm gains meaning; Parchment card by day), and a header chip "also in the … menu ⇄" deep-linking to the same section in the other state.

### Seasonal & specials

**Both states carry the Seasonal rail (built Sep 2026).** The strip was food-only in the build; it now renders on the drinks page too, with the kitchen's six cards as placeholders until the team supplies the bar's seasonal picks (same card shape: photo, name, "In …" link). On the drinks page the card links cross to the food page's sections. The title follows the season ("Summer Menu Highlights" is a placeholder, author Sep 2026) and is shared by both rooms — it names the season, not the room.
Seasonal is a **tag, not a section**: the dish lives in its natural category with a Lime badge, and a "Seasonal now" rail near the top is a query over the tag (decided after years of separate seasonal print pieces — integrated seasonal dishes measurably outperform them). Holiday/weekend specials: a `special` tag with an end date feeds the **Hot deals band** — a Lemon colour-block moment. Check: on day the ticker band and the deals band are both Lemon; the striping rule wants one colour-block moment per page — resolve at build (candidates: deals band steps to Cream, or the two never share a viewport).

### Bar's notes & the pairing station
- **The bar's note:** a static Paper slip beside *select* sections (breakfast → coffee of the week, grill → infusions, desserts → sweet vermouth). Always a person speaking, never "pairs well with X." One per section max, not every section; on dark grounds it's the brightest object in view — max one per viewport.
- **The pairing station:** the page's one interactive element, at the food → drinks seam — the About pairing picker reused: pick a dish, the bar answers with a printed ticket ending "jump to it →" (it doubles as the transition into the drinks state). Signature dishes may carry a small "ask the bar" chip that scrolls to the station and pre-picks the dish — one mechanic, one location, many entrances. The menu lists themselves stay a fast reading surface.

### Menu lists
Dotted-leader rows never run the full 12 columns. Dense sections run **two columns of rows** (the Grid long-form rule applied to lists); the leftover zone gets a job — a section photo or a bar's note. Exact times are allowed here (service-page exemption to the no-clock rule): "served till 12", "weekday lunch 12–16".

### Open (menu page)
- "Bar Snacks" — working name (see Section labels 🔶): decide vs "To Share" / "Snacks & Boards". *(Open test — see `testing.md`.)*
- Night **kitchen** word colours: latest mockups run the kitchen list in the lemon/lime family at night while the bar list runs Paprika — either unify on Paprika defaults or sanction per-room accent families at night and document the rule (watch that shared-green stays distinguishable).
- Double-Lemon on day: photo-stack sheets vs deals band in one viewport (see Seasonal & specials).
- Scalloped door edge — liked, not yet committed.
- Item-card anatomy (photo-per-dish vs rows + one section photo) — leaning rows + one 3:2 per section.

## Don't (web-specific)

- No clocks, countdowns, or timers in the hero.
- No generic web-template layouts (default bento, stock hero-with-photo-right).
- Buttons are the only boxed actions; chips the only inline actions; doors the only state switches (Menu page → Doors). No fourth tappable style.

General brand do/don't: `design.md` §8.
